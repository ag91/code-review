#+TITLE: Improvements plan for code-review mode

Goal: make reviewing AI-generated PRs from Emacs practical. See the
*important* changes early, keep noise away, and review inside real
repository context (xref, tests, running code).

* Phase 1 — Local repo context                                  :STATUS:done

The mode has no notion of "the PR's repository on disk".  Visiting a
file used =(magit-toplevel)= on the *review buffer*, which only works
when you happened to launch from inside the repo.  Fix: resolve,
clone if needed, fetch PR refs, and check out a worktree.

- New file =code-review-repo.el=:
  - =code-review-projects-root=: default dir to look for clones
    (~ =nil= disables).  Matches clones by git remote URL, one and
    two levels deep, plus the current =magit-toplevel=.
  - On-demand clones (=git clone --filter=blob:none=, prompted) go
    under =code-review-projects-root= (or =/tmp=) directly at
    =ROOT/REPO= (just the repository name).  Per-PR worktrees live
    inside the clone's git directory (=.git/code-review-worktrees/N=),
    so they never pollute the working tree and survive =git clean=.
  - Fetch =pull/N/head= (GitHub) or =merge-requests/N/head= (GitLab)
    into =refs/remotes/code-review/N/head=, plus the base ref.
  - =git worktree add --detach= at the PR head, hard-updated on
    refresh when the PR is force-pushed.
- Review buffer gets =default-directory= set to the worktree, so
  =project.el=, compile, grep, eglot etc. work against the exact PR
  version.  A "Worktree:" header shows it (clickable, opens Dired);
  =w= opens the worktree.

* Phase 2 — Transparent xref in the diff                          :STATUS:done

- =code-review--resolve-worktree-position=: shared resolver
  (file . line) for the patch line at point (ignores comment lines
  inside hunks).
- =code-review-xref-find-definitions= / =code-review-xref-find-references=
  (=M-.= / =M-?=): symbol at point in the diff, jump to the worktree
  file at the matching line, then run the real xref command there.
  No glue: the worktree is a normal project checkout, so every xref
  backend (eglot, etags, gtags) just works.

* Phase 3 — Noise reduction                                    :STATUS:done

CONSTRAINT (why this phase is classification, not replacement):
comment anchors (=position=, =side=, =line=) refer to the API's
unified diff.  Rendering a *different* diff (e.g. a locally
generated =-w= one) misplaces or drops comments.  So the review
surface stays the API diff; noise handling only classifies, tags
and collapses.

DONE (implementation notes):

- =code-review-diff-file-order-rules= now accepts regexp strings
  (order-only, backward compatible) or plists
  =(:match REGEX :tag TAG :collapse BOOL :hide BOOL)=.
- New built-in =code-review-diff-noise-rules= (enabled by
  default, battery included; set to nil to disable): lockfiles,
  minified/build output -> =GEN=; CHANGELOG/docs/README ->
  =DOC=; all collapsed.
- =code-review--diff--classify-diff= per render: merges user
  rules, noise rules, textual detection and the worktree check
  into a hash table used by the wash (tags, collapse) and by
  focus-mode filtering.  Classification happens on every render
  and is deliberately NOT permanent-local (the re-activation of
  =code-review-mode= at the end of =code-review--trigger-hooks=
  kills buffer-locals; everything reads it during the wash, so
  this is fine).
- Whitespace-only files: textual pairwise hunk comparison
  (=code-review--diff--block-ws-only-p=) plus, when the worktree
  exists, one =git diff --no-renames -w --ignore-blank-lines
  --numstat base...HEAD= call
  (=code-review-repo-substantive-files=): files absent from the
  numstat output (all-zero) are =WS-ONLY=, collapsed + hidden in
  focus mode.  Binary/mode-only blocks are guarded via
  =code-review--diff--block-has-content-p=.
- Pure renames -> =[MOVED]=, collapsed, NOT hidden in focus mode.
- Files carrying review comments are never auto-collapsed
  (=code-review--diff--file-has-comments-p= guard in the
  =insert-file-section= override).
- Ordering: user rules first, then unmatched files, then noise
  rules last (noise sinks to the bottom of the diff).
- Focus mode: buffer-local =code-review-focus-mode= (permanent
  so it survives re-renders), =N= or the transient toggles it;
  hidden files are removed from the buffer text but always
  counted in the "Files changed" heading
  (=code-review-section--focus-summary=).
- =V=: view-only =*Code Review Diff (-w)*= buffer
  (local =git diff -w --ignore-blank-lines base...HEAD=, =diff-mode=,
  warning banner that comments cannot anchor there).
- =D=: difftastic drill-down via
  =(difftastic-git-diff-range "refs/remotes/code-review/N/base...HEAD" nil (list FILE))=
  from the worktree; =C-u D= for the whole PR.  Soft-dependency:
  friendly install hint when =difftastic= or =difft= is missing
  (both absent on this machine; the error path is tested).
- GOTCHA fixed along the way: PR 3's API diff uses NO a/ b/
  prefixes (=diff --git file file=), so
  =code-review--diff--extract-b-path= now handles both forms;
  without this every file classified under path "".
- GOTCHA: docstrings containing ='(= need =\\='= in source to
  satisfy the byte-compiler docstring check.

Original design (kept for reference):

** File rules: from ordering to classification

Extend =code-review-diff-file-order-rules= so each element is
either a regexp (backward-compatible: order-only, as today) or a
property list:

  (:match REGEX :tag "docs" :collapse t :hide t)

- =:tag= shows a =[\[DOC]]= style tag on the file heading
  (=code-review-wash-insert-file-section= builds the file
  sections, so heading + default hidden state are easy to
  control).
- =:collapse= starts the file section hidden.
- =:hide= omits the file entirely in "focus mode" (never silently:
  the files-changed summary counts them).
- Built-in default rules (toggleable via
  =code-review-diff-noise-rules=): lockfiles
  (=package-lock.json=, =yarn.lock=, =pnpm-lock.yaml=,
  =Cargo.lock=, =flake.lock=, ...), minified/build output,
  CHANGELOG and pure docs -> =GEN= / =DOC= tags, collapsed.

** Hunk-level classification (purely textual, no repo needed)

- Whitespace-only hunks: every =-=/=+= pair is equal after
  collapsing internal whitespace -> =[\[WS]]= tag, collapsed by
  default.  Works on the raw diff text we already split per file
  in =code-review--maybe-reorder-diff=.
- Pure renames (header has =rename from/to=, no hunks) -> one-line
  =[\[MOVED]]= section.

** Local-repo assisted classification (phase 1 gives this free)

- When the worktree is available: =git diff -w base...head -- FILE=
  empty -> whole file tagged =[\[WS-ONLY]]= and collapsed (catches
  reformatting churn the textual check misses).

** Focus mode

- A transient toggle (like the existing =toggle-display-*= ones):
  hide everything tagged =GEN= / =DOC= / =WS-ONLY=; the
  "Files changed" heading reports what is hidden
  ("6 files: 3 substantive, 2 docs, 1 generated").

** The =-w= "what really changed" view, done safely

- A view-only buffer (=*Code Review Diff (-w)*=) rendering the local
  =git diff -w --ignore-blank-lines base...head=, banner noting
  that comments cannot anchor there.  This replaces the original
  (unsafe) idea of swapping the review surface to the =-w= diff.

** Difftastic drill-down

- Optional dependency on =difftastic.el= (pkryger, MELPA): more
  robust than a hand-rolled =GIT_EXTERNAL_DIFF= runner (it is the
  maintained version of that blog code: ANSI->theme faces, width,
  reflow, chunk nav).
- Key on a file section (say =D=): run
  =difftastic-git-diff-range= on base...head for that file, from
  the worktree (=default-directory= already points there).  With
  =C-u=: whole-PR structural diff.
- Explicitly NOT in-place rendering (rschmukler/magit-difftastic
  renders inside magit buffers with staging; in-place difftastic
  would break the line anchoring comments need).

* Phase 4 — Robust GitHub data (pr-review lessons)               :STATUS:done

How /tmp/emacs-pr-review handles comments (it *does* use magit, but
only as a renderer):

- Renders the diff with public =magit-wash-sequence= over owned
  washers (=code-review-wash-diff= since phase 11b).  No
  =advice-add= overrides of magit internals anymore.
- After washing, walks the buffer once and stamps every diff line
  with =pr-review-diff-line-left/right= text properties carrying
  =((path ...) (line ...))=.  Anchoring a comment becomes an exact
  =text-property-search-forward=, not line-count arithmetic.
- Comments come from GraphQL =reviewThreads= (=path=, =diffSide=,
  =line=, =isOutdated=, =isResolved=).  In-diff fringe markers link
  to full threads; pending comments are collected buffer-locally
  and submitted as one =addPullRequestReview= mutation with a
  =threads= input.

Plan for code-review:

- Fetch comments via GraphQL =reviewThreads=; kill the
  "amount-loc"/"head-pos" line arithmetic.  Anchor by (path, side,
  line) directly.
- New comments become pending review threads, submitted in one
  mutation.  Add resolve/unresolve support.

DONE (implementation notes):

- Both GraphQL queries now fetch =reviewThreads(first: 100)= with
  =id=, =isResolved=, =isOutdated=, =path=, =diffSide=, =line=,
  =startDiffSide=, =startLine= and their comments'
  =databaseId=/=fullDatabaseId=.
  - SCHEMA GOTCHA: =side= / =startSide= do NOT exist on
    =PullRequestReviewComment= (the query errors out and you get
    zero comments).  The side lives on the *thread*
    (=diffSide=/=startDiffSide=).  =code-review-github-fix-infos=
    now merges =threadId=, =isResolved=, =side=, =startSide= and
    =line=/=startLine= fallbacks from each thread into the review
    comments, keyed by =databaseId=.
- =make-group= keying precedence: outdated -> position key (render
  from own diffHunk); has line -> exact =path:side:line= key (no
  buffer arithmetic); else legacy position key.  Comment sections
  carry =thread-id= / =resolved?= (heading shows a RESOLVED tag).
- =wash-hunk= tries both RIGHT and LEFT line keys for context
  lines.
- =R= toggles resolve/unresolve via GraphQL
  =resolveReviewThread= / =unresolveReviewThread=
  (=code-review-threads-toggle-resolved=; fallback user-error for
  other providers).
- Validated: unit tests (merge, keying for modern/outdated/legacy/
  local comments, fallback) + live against real PRs
  (wandersoncferreira/code-review#96, ag91/code-review#3 with a real
  outdated thread: thread metadata merged, comment rendered from
  its diffHunk).
- NOT done here: converting local (pending) comments to a single
  =addPullRequestReview= mutation; that needs the write path
  redesigned (see pr-review).  Current write path already uses the
  line/side API.

* Phase 5 — Duplicate and dead code detection                   :STATUS:done

NOTE (phase 12): detection works in LOCAL diff reviews too
(=M-x code-review-review-local-diff= buffers) — reviewing your
own changes for duplicated code before committing is a main use
case.  It operates on the diff text and the repo, both available
locally.

DONE (implementation notes):

- =code-review-analysis.el=: pure engine.  Similar code = normalize
  added lines (mask strings/numbers/comments), 4-line shingles,
  match against an index of repository files.  Dead code = (a)
  added definitions with zero references in the worktree ->
  "possibly dead"; (b) deleted definitions still referenced ->
  "dangling".  Results cached per (PR id, diff md5) so re-renders
  are instant; =code-review-analysis-reset= clears the cache.
- Rendering: =code-review-analysis-section= class +
  =code-review-section-insert-analysis= in =code-review-sections-hook=
  (between Commits and Files changed).  Only inserted when there
  are findings; clearly marked "(heuristic)"; jump buttons to
  worktree files.
- PERFORMANCE (measured on litellm #41667, 11k files):
  - NEVER grep the packed base tree: =git grep <rev>= inflated every
    blob and froze the user's live daemon for 106 s (C-g incident —
    this is unacceptable, see Gotchas in AGENTS.md).  Candidate
    discovery greps the WORKTREE instead.
  - =git grep -n -F -f probes= scans every match of every probe:
    106 s.  =git grep -l= stops at the first match per file: 1.6 s.
    Hit-count ranking is lost (walk order instead), accepted.
  - References: ONE batched =git grep= for all definition names
    (was one grep per name: 46 greps).  2 s.
  - Hard caps: 250 probes, 100 candidate files, 3 MB index budget.
  - Total worst case ~3.7 s synchronous (litellm-sized repo);
    small repos are instant.  Findings on the real litellm PR:
    similar=0 dead=36 dangling=1 (the 36 possibly-dead defs on an
    AI-authored PR is a useful heuristic on its own).
- SELF-MATCHES: diff files are NOT excluded from candidates (a PR
  copying code between two files both in the diff is the primary
  similar case); each analyzed file's hits against its own
  worktree copy are filtered in =--analyze-one-file=.
- Gotchas found: =magit-git-output= breaks with glob pathspecs
  (magit sets =GIT_LITERAL_PATHSPECS=1=; use plain =call-process=);
  match-data is GLOBAL — bind match-strings before any nested
  =string-match=; cl-loop =unless= BEFORE a =for= clause does not
  filter (use =cl-remove-if= on the list); using a =_=-prefixed
  cl-loop destructure variable as a value is a =void-variable=
  error.  Details in AGENTS.md.
- UX: the analysis runs synchronously inside the render.  Worst
  case ~3.7 s on a huge repo; instant on normal ones and cached
  afterwards.  Async idle-sliced rendering or an on-demand
  keybinding are possible follow-ups if this ever feels slow.

TUNING (scalafmt#5264 review, 2026-09-20): on a real scala PR
the raw heuristics produced noise, and three rules fixed it:

- "possibly dead: FileHeaderTest/FileHeaderConfigTest" was a
  FALSE POSITIVE: *Test classes are invoked by BUILD-TOOL
  CONVENTION (sbt/munit/pytest), zero references is normal.  New
  defcustom =code-review-analysis-dead-test-name-regexp= (names
  ending Test/Tests/Spec/Suite/Case/IT, =test_*=/=testX= prefixes)
  skips them in the dead check.
- "similar" findings were IMPORT BOILERPLAPE: test files all
  start with the same import block, so a new test file "matches"
  every other test file at 12/574 covered lines.  New defcustom
  =code-review-analysis-boilerplate-line-regexp= (import/from/
  package/using/require/#include) drops such lines from the
  index, the added-side shingles AND the grep probes.
- An absolute covered-line count alone is meaningless on big
  additions: new =code-review-analysis-min-covered-ratio= (0.1)
  also requires the match to cover a fraction of the file's
  added lines.
- Verified on the real scalafmt worktree (analysis cache reset +
  rerun in the daemon): before 6 similar + 2 dead; after
  3 similar (11-16% coverage, FileHeaderConfigTest only) and
  0 dead.  The remaining 3 are munit-scaffolding-shaped; raise
  the ratio defcustom to 0.2 to silence them if still noisy.

** Bugfix: megabyte single lines killed the render (litellm incident)

BerriAI/litellm PR 43042: opening the PR died with "Got an error
from your VC provider" and no buffer.  The log said
=(error "Stack overflow in regexp matcher")=.  Triage (batch
emacs against a copy of the db + the real worktree) pinpointed
=code-review-analysis--normalize-line='s string-literal mask on a
497,412-character single line in
=cookbook/Benchmarking_LLMs_by_use_case.ipynb=: the matcher
recurses per character and overflows.  The error escaped through
=insert-analysis= into the deferred chain, which reported it as a
VC error — a local heuristic bug masquerading as a forge failure.

Fixes (both regression-tested):
- =code-review-analysis-line-max-length= (2000): every analysis
  regexp goes through =--cap-line= first, and =--read-files=
  skips monster-line files entirely (notebooks/minified bundles
  are data, not reviewable code — indexing them only burned the
  byte budget: 100 candidates dropped from 1.78MB to 1.0MB of
  real source).  The reference PRESENCE check still searches the
  full raw line via plain =string-search= (not a regexp, no stack
  risk): a reference buried at offset 150K inside a notebook line
  is still a reference, and capping that check produced false
  "dead" findings.
- =code-review-analysis-run= now condition-cases the compute: a
  failing analysis is LOGGED and reported as no findings.  It
  must never take the review buffer down with it.

Verified live: litellm#43042 renders fully (237 sections, 5
files, 14 hunks, analysis section present, 3 fringe thread
markers), batch acceptance on all three stored db rows of the PR
green, 103/103 ERT.

* Phase 6 — Edit submitted comments and PR lifecycle          :STATUS:done

The biggest write-path gap vs pr-review: everything it can edit
after submission.

DONE (implementation notes, 2026-09-21) — everything except the
"consider" bullet:

- EDIT SUBMITTED COMMENTS: =e= on any comment section that was
  already submitted to the forge (diff-anchored =code-comment= /
  =outdated= sections and Conversation =comment= sections)
  opens the comment buffer prefilled with the RAW MARKDOWN body;
  =C-c C-c= sends the edit and fully refreshes the review
  (=code-review-section-full-refresh?= => refetch, so the buffer
  re-renders with the server's new bodyHTML).  A conversation
  entry with typename =PullRequestReview= edits the submitted
  REVIEW SUMMARY.  Guards: local diff reviews user-error;
  comments authored by someone else ask =y-or-n-p= (the forge
  rejects them anyway); missing raw body (buffer rendered from
  pre-phase-6 DB data) tells you to press =G= first.
- Data plumbing: the =body= (raw markdown) field was added to
  BOTH GraphQL queries in three places each (review nodes, review
  comment nodes, top-level comment nodes); new =body= slot on
  =code-review-base-comment-section= and
  =code-review-comment-section=; =make-group= and the GitHub
  conversation inserter pass it through.
- ENDPOINTS: REST equivalents of the roadmap's GraphQL
  mutations, chosen deliberately — =updatePullRequestReviewComment=
  == =PATCH pulls/comments/{id}=, review summary ==
  =PUT pulls/N/reviews/{id}=, IssueComment ==
  =PATCH issues/comments/{id}=.  REST keys on the plain
  =databaseId= the comment sections already carry (exactly like
  the existing delete-code-comment path); the GraphQL mutations
  would need extra global node ids fetched for every comment.
  Selection lives in a pure, tested helper
  (=code-review-github--update-comment-endpoint=); the generic
  =code-review-update-comment (obj kind comment-id body callback)=
  has a provider fallback user-error.
- REOPEN: =o= in the transient; =code-review-reopen-pr= guards
  non-GitHub and non-CLOSED PRs, then PATCHes the pull request
  with =state: open= (new generic =code-review-reopen=).  The
  existing close path sends uppercase =CLOSED= and works, but
  the documented REST value is lowercase, so reopen sends
  ="open"= explicitly.
- DRAFT TOGGLE: =d= in the transient; =code-review-toggle-pr-draft=
  flips via GraphQL =convertPullRequestToDraft= /
  =markPullRequestReadyForReview= (input: =pullRequestId=, taken
  from =raw-infos= id — no REST equivalent exists), then
  optimistically flips =isDraft= in raw-infos, persists and
  rebuilds (the "Draft" header re-renders from it).  New generic
  =code-review-toggle-draft= with provider fallback.
- TESTS: 11 new ERT tests (make-group carries raw body;
  =code-review-comment--edit-target= kinds incl. the local/reply
  and outdated cases; helper-header stripping; user-errors for
  no-comment-at-point and local reviews; endpoint selection x3;
  both GraphQL queries fetch =body=).  Full suite 81/88: the 7
  failures are the hunkhighlight face tests, which ALSO FAIL ON
  UNMODIFIED HEAD on this Emacs 30.2 machine — they were
  developed and verified on Emacs 31 (other machine), where the
  treesit runtime differs (Emacs 30.2 supports only
  equal/match/pred at capture time).  Pre-existing, not touched
  by phase 6.  (FIXED 2026-09-22: see EMACS 30/31 COMPAT under
  phase 10 — the suite is 89/89 on this machine now.)
- GOTCHAS found (recorded in AGENTS.md where general):
  - EIEIO =-p= predicates are EXACT-CLASS checks (at least on
    this Emacs 30.2 daemon):
    =code-review-base-comment-section-p= returns NIL for a
    =code-review-code-comment-section= instance, while =cl-typep=
    is subclass-aware.  Never dispatch on parent-class
    predicates; enumerate the concrete classes.  Also the
    =local?= SLOT lies: =code-review-outdated-comment-section=
    sets =local?= t even for comments FETCHED from the forge —
    classify by class, never by that slot.
  - =(cl-letf (((fn) val)) ...)= needs a =(setf fn)= expander and
    signals =void-function ((setf fn))= in batch tests: rebind
    with =fset= + =unwind-protect= instead.
  - The verification incident: a probe script that walked ALL
    sections of ALL live review buffers via =magit-map-sections=
    hung the daemon for minutes (user had to C-g; the tree walk
    itself is linear, so the suspect is stale section objects
    whose classes had just been redefined).  This is the
    "NEVER run heavy/unbounded compute in the live daemon" rule
    in AGENTS.md biting again: the minimal reload script (loads
    + keymap patches + report into a defvar) is instant.  Verify
    with bounded, targeted probes only.
  - Daemon =defvar= keymaps again needed explicit =define-key=
    patches after reload (the =e= binding); the new defcustom
    needs no rebind since it did not exist before.
- NOT done: converting the local-comments submit to a single
  =addPullRequestReview= mutation with a =threads= input (the
  "consider" bullet).  Same conclusion as phase 4: needs the
  write path redesigned around pending threads; the current
  line/side review submit stays.

* Phase 7 —  PR search                :STATUS:todo

Cuts the GitHub-website round trips.

- =code-review-search= / =code-review-search-open=: search PRs
  by GraphQL query, pick from results (completing-read or a
  listview buffer).
- =browse-url= integration: register a URL parser in
  =browse-url-default-handlers= so GitHub PR links (with file/line
  anchors) open inside Emacs; also find a PR URL in the current
  buffer (email/notmuch workflow).

DONE (2026-09-21) — the browse-url bullet only; the two bullets
above remain open.

- New =code-review-browse.el=.  =code-review-browse-url= is
  registered in =browse-url-default-handlers= for github.com PR
  links, so PR links clicked anywhere (notmuch, eww, org) open
  inside Emacs instead of the browser.  Registration happens at
  load time (idempotent; =code-review-browse-url-install= /
  =-uninstall= for manual control) AND at startup through an
  autoload cookie (=(with-eval-after-load 'browse-url ...)= form
  in the regenerated =code-review-autoloads.el=), so it works
  before any code-review command has ever run.
- Anchors understood: =#diff-<sha256-of-path>= — the same digest
  =code-review-jump-to-gh= builds outbound, used in reverse: file
  sections are hashed until one matches (prefix match, so
  truncated GitHub hashes work); =L<n>= / =R<n>= old/new-side line
  anchors (a line exists on a side only when it is context or
  +/- respectively — the naive "counter equal" check happily
  lands on a deleted line when jumping to the new side);
  =#discussion_r= / =#issuecomment-= / =#pullrequestreview-= ids
  matched against section ids (the dual-role comment sections
  carry theirs on the VALUE object, not the rendered section).
  Unknown anchors open the review at the top with a message.
- Reuse before fetch: when =*Code Review: owner/repo#N*= is
  already open, the handler pops that buffer and jumps
  immediately (no refetch; the pending list stays empty).
  Fresh opens go through =code-review-start= with the
  canonicalized URL (fragment/query//files/... stripped so the
  URL stored on the PR object stays clean for outbound links);
  the anchor jump runs from =code-review-post-hook= once the
  buffer is rendered (=code-review-browse--after-render= was
  added to that defcustom's DEFAULT — live daemons need the
  standard-value rebind).
- =code-review-open-pr-at-point=: URL at point via
  =thing-at-point=, else scan the whole buffer for PR links of
  all three forges (deduped, =completing-read= when several) —
  the email/notmuch workflow; fragments are kept intact so
  notification-email anchors still jump.
- LATENT WASH BUG FIXED on the way: =code-review-wash-hunk= read
  =(match-string 0/1/2)= AFTER db writes.  On a cold emacsql
  statement cache those writes clobber the global match data and
  the reads returned garbage or signaled =args-out-of-range=.  It
  had gone unnoticed because the db ERT tests run early in the
  suite and warm the statement cache; the browse tests are
  alphabetically FIRST and hit the wash cold.  The hunk header
  groups are now bound immediately after =looking-at= (gotcha
  recorded in AGENTS.md).
- Tests: =test/code-review-browse-test.el=, 13 ERT tests
  (fragment parsing, canonicalization, buffer names, handler
  regexp, idempotent registration, file/line/comment jumps incl.
  truncated hashes and out-of-body lines, email scan).  Full
  suite 77/77.  Verified live in the daemon on
  writer-data-platform#762: file anchor lands on the exact file
  section, =R366= on the real diff line, =discussion_r= on the
  matching id, =browse-url= dispatch end-to-end, and
  =code-review-open-pr-at-point= from a scratch email buffer; no
  refetch happened (pending stayed nil) and the window
  configuration was restored.

SLACK CLICK POISONED THE DB (2026-09-25 incident, fixed).

- Symptom: clicking a GitHub PR link in a slack (emacs-slack/lui)
  buffer, then any use of the review db, failed with
  =EmacSQL had an unhandled condition: "#<"= — the read-back
  error from an unreadable object embedded in a stored value.
- Root cause: lui link buttons hand =browse-url= a URL STRING
  CARRYING TEXT PROPERTIES (=lui-raw-text= with the whole
  message, plus keymap properties holding slack team/channel
  objects).  =match-string= PRESERVES properties, so
  =code-review-browse--canonical-url= and
  =code-review-utils-pr-from-url= produced propertized
  owner/repo/num/url — and =emacsql-escape-scalar= encodes
  scalars with =prin1-to-string=, so the INSERT wrote the entire
  property payload (15-49MB of printed slack state) into the
  pullreq columns.  Six such rows accumulated (PR 780 "pepper"
  x3, PR 794 "pine" x2, PR 1295 x1); the db file ballooned to
  672MB.  Every later read of a poisoned row (=read= on the
  stored text) died at the first embedded =#<buffer ...>=.
  The click itself created the row and immediately read it back
  (=build-buffer= -> =closql-get=), so the error fired right at
  open time; clean =code-review-start= calls worked, which is
  what misdirected the initial suspicion.
- Fix: strip text properties at the parse boundary —
  =code-review-utils-pr-from-url= rebinds =url= to
  =substring-no-properties= (every slot it returns flows from
  there), and =code-review-browse--canonical-url= returns a
  property-free canonical URL.  Regression:
  =code-review-utils-test/pr-from-url-strips-text-properties=
  (propertized slack-style URL parses clean; every string in the
  alist asserted property-free).  Suite 104/104.
- Repair: db backed up (=code-review-db-file.sqlite.bak-slack-props=),
  the 6 poisoned rows deleted (no child rows ever referenced
  them), =VACUUM= shrank the file 672MB -> 43MB, integrity ok,
  222 real rows intact, and the previously explosive
  =code-review-db-all-unfinished= scan runs clean.  Verified
  live in the daemon: propertized URLs parse clean, the 780
  buffer's row reads ("780" "OPEN"), zero poisoned rows remain.
  Delete the backup file once satisfied.

* Phase 8 — Diff drill-down: ediff and base views                :STATUS:todo

Nearly free now that phase 1 gives us the worktree and the base
ref.

- =code-review-ediff-file=: ediff the changed file under point,
  worktree (head) vs =refs/remotes/code-review/N/base= checkout
  of the base version.
- View the file under point at the *base* version (currently we
  only have head, via the worktree).
- =code-review-select-commit=: narrow the reviewed diff to a
  subset of the PR commits (complement to the existing commit-focus
  buffer).

* Phase 9a — Reading ergonomics for AI-generated PRs               :STATUS:done

Reviewing a PR authored (and reviewed) by bots means thousands of
CI-check lines and bot-to-bot chatter bury the code.  The buffer
now starts folded to the useful parts.

DONE (implementation notes):

- =code-review-fold-header-sections= (default =t=): Commits, each
  commit's CI checks (grouped behind a single =CI Checks (N)=
  heading), Description, Your Review Feedback, and Conversation
  start folded.  Implemented by passing HIDE to the relevant
  =magit-insert-section= calls and wrapping each with
  =code-review-section--hide-if-hidden=, which creates the real
  invisible overlay (this package renders directly, so
  =magit-refresh-buffer= never does it for us).
- =C-c C-n= / =C-c C-p= (=code-review-next-hunk= /
  =code-review-previous-hunk=): jump between diff hunks, skipping
  comment sections and files collapsed as noise.  The
  promote-to-issue binding that used to sit on =C-c C-n= in the
  comment map moved to =C-c C-i=.
- =code-review-collapse-bot-comments= (default =t=) plus
  =code-review-bot-author-regexp= (matches =[bot]= suffix,
  github-actions, devin, cursor, copilot, gemini, codeium,
  coderabbit, codspeed, greptile, codecov, claassistant,
  renovate, dependabot, imgbot, semantic-release,
  all-contributors; case-insensitive): bot comments render
  collapsed to their
  one-line heading.  A bot thread that contains a human reply
  stays expanded.  Run from =code-review--trigger-hooks= after the
  wash, so it needs no refresh machinery.
- Sticky file name: =code-review--update-header-line= (on
  =post-command-hook= and =window-scroll-functions=, buffer-local)
  shows the current hunk's file in the header line only while the
  real file heading is scrolled off screen; otherwise nil.
- Diff colors restored, twice over:
  - =magit-wash-sequence= loops while the washer returns NON-NIL.
    The file override's last form became the phase-9a
    =hide-if-hidden= wrapper (a =when= that evaluates to nil for
    visible sections), so the file wash silently STOPPED after
    the first file: the PR's other 11 files stayed in the buffer
    as raw, uncolored diff text.  The override now returns the
    section object.  Rule: in any function called from
    =magit-wash-sequence=, the return value drives the loop.
  - Hunks are also painted eagerly right after each wash via
    =code-review-wash--paint-hunk= (see gotchas below);
    current magit only paints lazily through machinery that
    never runs in these buffers.

Gotchas discovered (they will bite again):

- HIDE in =magit-insert-section= only applies when a section is
  created fresh: on re-render the previous visibility is
  inherited from the matching section of the old tree
  (=magit-insert-section--oldroot=).  That is desirable (TAB
  state survives a sync), but it means default folds show up on
  new buffers only, and old pre-feature buffers keep their
  unfolded state until manually folded.
- The inserted section object keeps its own slots unbound; the
  prebuilt object passed to =magit-insert-section= lands in the
  =value= slot (comment authors live at
  =(oref (oref section value) author)=).  Reading a slot a class
  does not have signals =invalid-slot-name= instead of returning
  nil, so guard with =slot-exists-p= + =slot-boundp=
  (=code-review--section-author=).
- Recursive =(let ((walk (lambda ...))))= self-calls are void
  under lexical binding; use =magit-map-sections= (it exists as a
  function in current magit-section) or a named defun.
- =(defclass name (parents) "doc")= puts the docstring in the
  slot-list position; eieio iterates it char by char and signals
  =(wrong-type-argument symbol 71 slot)= (71 is ?G of "Groups").
  The slot list comes first: =(defclass name (parents) ()
  "doc")=.
- Diff colors (magit 4.x): hunk painting moved to
  =magit-section-paint=, driven lazily by the section-highlight
  machinery that never runs here (=magit-refresh-buffer= absent,
  =magit-section-highlight-current= off, and the =painted= slot
  unbound).  Paint eagerly instead, right after the wash:
  =magit-insert-section= RETURNS the section (with its =end=
  marker already set, which the painter needs), so wrap the call.
  The painter expects point at the section start (it only does
  =forward-line=), so =goto-char= there first.  Also remove the
  old =:washer= argument from hunk creation: current magit treats
  a bound washer as a lazy-body inserter and would corrupt the
  =content= marker on TAB.
- Comment classes are dual-role: the same EIEIO class
  instantiates both the prebuilt data objects and the inserted
  magit sections (section.value = data obj).  Implement
  =magit-section-ident-value= for them so =magit-section-ident=
  keys the visibility cache by forge id.  The method MUST handle
  both roles (delegate to the value object when called on the
  section, return id/author when called on the data object);
  a single-role method shadows magit's fallback, returns nil for
  the section, and every comment again shares one ident — hiding
  one bot comment then re-hides ALL comments (humans included)
  on the next render.  With =magit-section-preserve-visibility=
  =t= (default), the old-tree visibility matching is OFF: the
  cache is the only cross-render state.
- In a live daemon, =defcustom= defaults and =defvar= keymaps do
  not update on reload (the running value wins); patch them with
  =set=/=define-key= when testing interactively.
- Stale native =.eln= caches shadow fresh =.elc=s: delete
  =~/.emacs.d/eln-cache/<emacs>/code-review-*.eln= (they
  regenerate) and verify behaviorally; a native function prints
  as =#<subr NAME>= so its body cannot be inspected.

* Phase 9 — Review ergonomics (pr-review polish)                 :STATUS:done

Three ergonomics features, all live-verified on a local-diff
render (and covered by ~code-review-ergonomics-test.el~):

** eldoc context — =code-review-eldoc-context=

Registered buffer-locally on =eldoc-documentation-functions= by
=code-review-mode=.  Inside hunks it reports
=file:new-side-line (kind)= where kind is =added=, =removed= or
=n= (= old m)= for context lines with differing old/new numbers;
the line numbers use the same counting as
=code-review-jump-to-line= (~--count-patch-advances~ over the
nearest patch line at point).  On a file header it reports the
stripped path; anywhere else (containers, root) it stays silent.

** Global folding — =C-c C--= / =C-c C-+=

=code-review-fold-less/-more= cycle a 1..5 scale managed by
=code-review-fold-show-level=.  The scale is by section KIND, not
raw nesting depth: the real render wraps files in TWO containers
(files-report, files-chnged) so magit's own
=magit-section-show-level -N= depth semantics shift between the
render and the test harness.  5 = everything, 4 = threads folded
(useful with bot-collapse), 3 = hunks folded, 2 = files folded,
1 = containers only.  The fold bypasses magit's visibility cache
(never remembered across renders).

** Fringe thread markers

=code-review--mark-comment-lines= runs from
=--trigger-hooks= after EVERY render (buffer erasure kills the
overlays).  It walks the section tree for comment sections, finds
each thread's anchor patch line (walk backwards from the section
start to the nearest patch line — the wash inserts threads right
after their anchor), and lays a zero-width overlay with a
=left-fringe= display glyph (violet chevron,
=code-review-comment-marker= bitmap +
=code-review-fringe-comment-face=).  Mouse-1 on the marked line
jumps to the thread and reveals its ancestor chain
(=code-review-fringe-jump-to-thread=).  Toggle:
=code-review-comment-fringe-markers=.

Gotchas found while building (also in AGENTS.md):
- ~magit-map-sections~ returns the section it walked FROM, not a
  list of results: collect via side effects.
- Re-defining a derived mode does not update pre-existing
  buffers: they keep the OLD keymap object and miss the new
  local hooks, so daemon verification must patch live buffers
  (=use-local-map= + =add-hook ... nil t=).

* Phase 10 — Tree-sitter semantic hunk highlighting                :STATUS:done

NOTE (phase 12): highlighting must work in LOCAL diff reviews
too (=M-x code-review-review-local-diff= buffers); the wash/paint
path is shared so it should inherit for free — verify with a
local buffer when implementing.

Not implemented yet; design sketch agreed so the next session can
pick it up.  The daemon runs Emacs 30.2 with =treesit= and the
python grammar available.

Goal: hunks read as code, not as colored diff text.  For a python
file, see the import, the =def= name, its arguments, the body,
dataclass fields; in test files, test names and assertions stand
out.

Design:

- After =magit-diff-wash-diffs=, each hunk section body is plain
  diff text inserted by our own washer.  Strip the =+/-/space=
  prefixes per line, reconstruct the new-side text of the hunk,
  parse it with =treesit-parser-create= + =treesit-query-capture=.
- Map node ranges back onto diff line ranges and lay
  =font-lock-face= overlays on the hunk body (our washer inserted
  the text, so we can property it directly; magit's diff faces
  stay as the base).
- Capture groups per language, python first:
  =font-lock-keyword-face= for imports and =def=;
  =font-lock-function-name-face= for def/class names;
  =font-lock-type-face= for dataclass/NamedTuple names and their
  field lines; =font-lock-variable-name-face= for args; test
  files (name or path match =test=) get a dedicated
  =code-review-test-face= on test def names and assertion lines.
- =code-review-semantic-highlight= defcustom (default =t= when
  =treesit-available-p= and the file's grammar is installed; nil
  otherwise).  No grammar means current faces, no error.
- Performance: parse per hunk during render, cache results keyed
  by a hash of the hunk text in a buffer-local table so re-renders
  skip the work; parsing is bounded by hunk size, not PR size.


BONUS POINT: keep this as a separate library and make sure it works
also with magit diffs, so the user can reuse this work for reading better diffs.

DONE (implementation notes, 2026-09-20):

- New library =code-review-hunkhighlight.el= (the bonus: it works
  on ANY magit-style diff buffer via
  =code-review-hunkhighlight-magit-buffer=, which walks the magit
  section tree from the ROOT — walking from the section at point
  only finds the hunk you are standing in).  In code-review
  buffers the wash calls the core directly per hunk.
- Pipeline per hunk: strip +/-/space prefixes, reconstruct the
  NEW side (added + context lines; deleted lines are not part of
  it), parse with treesit, run per-language queries, map each
  captured node back to review-buffer positions, and lay the
  semantic face as an OVERLAY over the line's diff face (see the
  post-mortem below: a merged face LIST does not display as
  hoped).
- Python first (validated against the installed grammar), with a
  face set the user tuned after seeing the first render: THE LESS
  THE BETTER — def/class names, parameters, assignment targets
  (lowercase), UPPER_CASE constants, and the =return= keyword only.
  =import= and every other keyword stay diff-colored.  Test files
  (path matches =code-review-hunkhighlight-test-path-regexp=) also
  get =code-review-test-face= on =test*= def names, the =assert=
  keyword and =assert*= call names.  Other languages = extend the
  =language-map= + =queries= defcustoms — but ONLY with queries
  validated against the installed grammar (compilation failures
  silently disable an entry; see the treesit gotchas in AGENTS.md).
- Graceful degradation everywhere: no treesit / no grammar /
  unknown language / =code-review-semantic-highlight= nil =>
  exactly the old faces, no error (the region core is
  condition-cased and never signals into the wash).
- Performance: parse per hunk (bounded by hunk size), compiled
  queries cached globally, face ranges cached per buffer keyed by
  LANG|test-p|md5(hunk body) so re-renders replay without
  reparsing.
- Verified: 7 new ERT tests (pure mapping + treesit integration,
  which skip themselves without the python grammar) green in
  batch AND in the live daemon; live local review of the demo
  repo shows =import= / =helper= / =never_used= merged on top of
  =magit-diff-added= in the real rendered buffer.

POST-MORTEM (same day): the "merged on top of" above was a lie
the buffer properties told us — the user saw ONLY the default
green/red faces.  Two display rules were at play:

1. A =font-lock-face= property whose value is a LIST merges with
   EARLIER faces WINNING attribute conflicts.  Our list was
   =(magit-diff-added font-lock-keyword-face)= and
   =magit-diff-added= sets its own foreground (#22aa22), so the
   diff foreground beat the semantic one.  The semantic face was
   present in the properties but invisible on screen — checking
   text properties said "works", looking at the buffer said "no".
2. Overlay faces OVERRIDE text-property faces (higher display
   priority), which is exactly what we want: semantic foreground
   on top, diff background still showing (the semantic faces set
   no background).

FIX: =code-review-hunkhighlight--put-face= now lays per-line
overlays (marker property =cr-hh-face= per face => idempotent,
different semantic faces stack, =evaporate= so re-renders clean
up).  Overlays also survive magit 4.x lazily REPLACING hunk face
text properties when you move through hunks, which a text-property
merge would not.  Tests assert overlay faces
(=overlays-at=) plus the base diff face; 54/54 green, demo buffer
re-rendered in the daemon and verified: =import= -> keyword
overlay, =helper= -> function-name overlay, 18 semantic overlays
in the buffer.  LESSON: verify faces by what the DISPLAY
resolves to (overlay > text property; earlier list face wins
conflicts), never by =get-text-property= alone.

TUNING (first user review, same day):

- Faces on UNCHANGED context lines were noise: the reviewer
  cares about what the patch TOUCHES.  `--apply` now paints
  ADDED lines only (it reads the diff prefix char before the
  content start; context lines still feed the parser, they just
  stay diff-colored, and deleted lines never reach the new side).
- The keyword face was everywhere (every =if= / =for= / =and=);
  trimmed to the user's minimal set: def/class names, parameters,
  variables, UPPER_CASE constants, =return=.  18 overlays in the
  demo buffer became 10 and the buffer reads far calmer.
- Gotcha found while trimming: =treesit-query-compile= ACCEPTS
  =#not-match=, but =treesit-query-capture= errors at runtime
  ("Emacs only supports equal/match/pred") — and since compile
  succeeds, a broken predicate used to kill the WHOLE language's
  highlights (the error aborted =--ranges=).  Two fixes: use a
  =#match= lowercase pattern instead of =#not-match=, and
  =--ranges= now captures per-entry inside a condition-case so a
  bad query disables only itself.  Note the =#match= anchors:
  ="\\`[a-z_]"= for plain variables vs
  ="\\`[A-Z_][A-Z0-9_]*\\'"= for constants.

SCALA + LITERALS (scalafmt#5264 review, 2026-09-20):

- SCALA queries added (all validated against the installed
  grammar, capture-exercised, node names probed first): def /
  class / object / trait names, =def= parameters, =val= targets
  (UPPER_CASE = constant, lowercase = variable, same =#match=
  anchors as python), =return=, and literals.  =language-map=
  now maps =.scala= / =.sc= / =.sbt=.
- LITERAL CONSTANT VALUES (strings, numbers) are highlighted in
  EVERY language with queries (user request): in test code they
  are the test case names and expected values — =test("comment
  starting with 'format: off' ...")= pops out.  Node names
  probed per grammar: python =string/integer/float=, scala
  =string= and =integer_literal/floating_point_literal= (scala
  has NO =number= node — that error only surfaces at capture
  time, compile is happy; the per-entry condition-case added
  earlier now contains such mistakes).
- Verified on the real =*Code Review: scalameta/scalafmt#5264*=
  buffer (safe path: =code-review-hunkhighlight-magit-buffer=
  walks existing sections, no rebuild, no comment loss): 959
  overlays (691 constant / 208 variable / 31 type / 22 function
  / 7 return), test-name strings get =font-lock-constant-face=,
  imports stay plain, added lines only.

ELISP + CLOJURE (ag91/code-review#8 review, same day):

- Both grammars probed the same way (capture-exercised, never
  compile-only).  The grammars are structurally different, which
  shaped the queries:
  - ELISP: =defun= forms are =function_definition= nodes with a
    named ="defun"= token child; =defvar=/=defconst=/=let= are
    =special_form=.  =defmacro= is NOT special (structure error
    at capture time) and =defsubst= leaks params into the name
    capture — both left out deliberately.  Queries: defun name +
    params, defvar, defconst, let bindings, =[(string)
    (integer)]= literals.
  - CLOJURE: everything is =list_lit= of =sym_lit=s, so
    definitions are matched positionally by the head symbol with
    =#match= (NOTE: =#eq= is NOT supported at capture time in
    Emacs 30.2 either — same rule as =#not-match=: compile is
    happy, capture errors).  Queries: defn-family names
    (defn/defn-/defmacro/defonce/defmulti/defprotocol/
    defrecord/deftype), def = variable, fn/let/loop vector
    symbols, =[(str_lit) (num_lit)]= literals.  The defn
    params pattern needs the vector IMMEDIATELY after the name
    (an =_= between them eats the vector and matches nothing).
- =language-map= now also maps =.el= and =.clj/.cljs/.cljc=.
- Verified: batch ERT for both languages (they run, not skip —
  grammars visible even in =emacs -Q=); live walker run on
  =*Code Review: ag91/code-review#8*= (no rebuild): 16 overlays
  (13 constants — strings/ints pop nicely in elisp — 3
  variables), 0 function names because that PR adds no =defun=s,
  which is the correct answer, not a failure.
- No clojure review buffer at hand yet: the walker covers any
  existing one (or just open one; the wash paints on render).

CONSTANT FACE (user readability, same day): the theme's
=font-lock-constant-face= is low-contrast on the green added
background ("dark green/light blue", hard to read).  All
constant captures (literal values AND UPPER_CASE constants) now
use a dedicated =code-review-constant-face=: =dark violet= bold
on light frames, =MediumPurple1= bold on dark ones — distinct
from the keyword purple and the function-name blue, and
customizable via =M-x customize-face=.  Both live buffers were
repainted (scalafmt: 691 constants, code-review#8: 13).  This
exposed a cache flaw: cached ranges carry RESOLVED faces, so a
face/query change silently repainted the OLD faces until the
buffer-local cache was cleared.  Fix: the cache key now hashes
the language's query+face defcustom entries together with the
hunk body, so any customization invalidates the cache
automatically.
TYPESCRIPT + TSX (writer-data-platform#762 review, 2026-09-21):

- Queries symmetric to the scala set, all capture-exercised
  against BOTH =libtree-sitter-typescript= and
  =libtree-sitter-tsx= (the tsx grammar shares every node name,
  so the =tsx= block repeats the strings verbatim).
  =language-map= now maps =.ts= / =.tsx=.  Covers: function and
  method names, class/interface/type-alias/enum names,
  parameters (required + optional, arrow functions included),
  lexical/variable declarations (UPPER_CASE = constant,
  lowercase = variable), for-of/for-in bindings, switch-case
  values + the =case= keyword, =return=, and
  =[(string) (template_string) (number)]= literals.
- Probing gotchas worth keeping: enum names are plain
  =identifier= (NOT =type_identifier=; class/interface/type
  alias names ARE =type_identifier=), and =for (const x of ys)=
  is FLATTENED — there is no =left: variable_declaration= child,
  the binding sits in the =left:= identifier slot of
  =for_in_statement= (one node for for-of AND for-in).  Both
  wrong guesses compiled fine and failed only at capture time
  (structure error) — probe node shapes first, as always.
- Test-file queries (jest/vitest): =describe=/=it=/=test= and
  =expect=/=assert= callees get =code-review-test-face= (they
  are plain call expressions, no dedicated grammar nodes).
- Verified live on =*Code Review:
  WriterInternal/writer-data-platform#762*= (the same PR that
  exposed the 11b wash bug): its =.e2e.test.ts= hunk shows
  =comparedColumns= as a variable, the SQL =template_string=s
  as constants and =expect= with the test face — 13 overlays.
  Batch: 3 new ERT tests (typescript, typescript test-file,
  tsx), 64/64 green.

EMACS 30/31 COMPAT (2026-09-22): the 7 face tests failed on
this 30.2 machine because the predicate spelling is
VERSION-EXCLUSIVE:

- Emacs 31 speaks the standard =(#match? @capture "REG")'=
  (capture first, question mark) — and =#match= does not even
  COMPILE there.  Emacs 30.2 only supports =(#match "REG"
  @capture)'= at capture time — =#match?= COMPILES happily and
  fails at capture ("Invalid predicate ... only supports equal,
  match, and pred"), so the per-entry condition-case silently
  disabled every predicate entry (const/var/test faces) while
  the plain entries kept working.  A version-exclusive spelling
  needs a RUNTIME rewrite, not a spelling swap.
- An empirical matrix probe (wrapped/unwrapped patterns, sibling
  captures, bare anonymous nodes, alternations, list form, both
  spellings, both argument orders — all capture-exercised on
  the real buffer) settled the 30.2 contract: every structural
  form works, ONLY the predicate spelling differs.  Earlier
  beliefs that unwrapped query strings fail ("Node type error")
  were artifacts of a broken probe (parser created in a
  different buffer than the text).
- Fix (all inside =code-review-hunkhighlight.el=):
  =--contract= probes ONCE by capture-exercising one tiny query
  of each spelling (compile alone cannot discriminate — the
  30.2 failure is at capture), memoized in a defvar;
  =--old-style-query= rewrites =(#match? @cap "REG")= to
  =(#match "REG" @cap)= string-surgically when the contract is
  old; =--compiled= normalizes BEFORE compiling and keys the
  compile cache by the NORMALIZED query (stale broken entries
  can never be replayed); the buffer-local ranges cache key now
  includes the contract, so already-rendered buffers recompute
  their predicate-less ranges instead of replaying them.
  Contract new (Emacs 31) passes the authored queries through
  untouched.
- Two traps while implementing (both now in AGENTS.md): =?= is
  a regexp QUANTIFIER — the rewrite never matched =#match?=
  until the literal =?= was escaped; and =match-string= without
  an explicit string argument inside
  =replace-regexp-in-string='s replacement lambda read the
  positions against the CURRENT BUFFER (the hunk parse buffer),
  splicing HUNK TEXT into the query strings ("Syntax error ...
  afmt._ class Foo(a: Int) rg.sca").  The scan is now a manual
  =string-match= loop with explicit string args everywhere.
- Verified: 89/89 green in batch on 30.2 (the 7 face tests now
  pass, plus a pure rewrite test asserting the string surgery
  on tricky escaped regexps); daemon reloaded with a bounded
  probe: contract = old, rewrite sample correct, and the ranges
  pipeline produces const/var/fn/kw faces on a sample text.
  On the Emacs 31 machine nothing changes (pass-through); run
  =make test= there to confirm.
- SQL and YAML support (writer-data-platform PR 795): SQL is
  performance-shaped — table references and dbt =ref()/source()=
  model names in type face, DDL/selected/WHERE/ON/GROUP/ORDER
  columns in variable face, clause keywords in keyword face, and
  cartesian joins in WARNING RED: =(cross_join ...)= keywords
  AND the comma of =FROM a, b= (a cross join in ClickHouse).
  YAML captures the VALUES of =name:= keys (dbt model/column
  names) in function-name face.  Probed every query against the
  DerekStride =tree-sitter-sql= grammar and the yaml grammar
  with the real PR hunks before shipping.
- Grammar gotchas found while probing (both bit in tests first):
  the DerekStride SQL grammar does NOT split newline-separated
  statements without =;= — everything after an unterminated
  statement is swallowed into one ERROR tail (test hunks now use
  realistic semicolon boundaries); and jinja ={{ }}= produces
  ERROR soup but clean text around it still captures (dbt
  =ref('mrt_one')= survives as an invocation literal) — graceful
  degradation, no special-casing.
- YAML fragments are structurally broken for the whole-fragment
  pass: a hunk starting mid-column-block loses the indentation
  context an indentation-relative grammar needs, and error
  recovery keeps only the FIRST pair (probed live on the real
  =_marts.yml= hunk: 1 capture out of ~10 name lines).  Dedent
  preprocessing (min-indent, first-line) probed and REJECTED —
  relative indentation survives text shifts.  Fix:
  =code-review-hunkhighlight-fragment-line-languages= (default
  =(yaml)=): every non-blank line is ALSO parsed as a one-line
  document and those captures merge; the ranges cache key
  includes the defcustom so toggling it recomputes.
- THE BIG ONE — a stale =.elc= lied for a whole session: the
  fragment pass was inserted with the wrong closer count (and the
  cache-key =cons= was one closer short), so
  =byte-compile-file= FAILED with "End of file during parsing" —
  and because =make compile= output was piped through a filter
  (=grep ... | head -n 5=), the error was invisible.  The stale
  =.elc= kept working, batch tests ran the OLD code, daemon
  probes loaded the OLD code, and the new test "failed" while
  every instrumented replica of the new logic worked.  Lessons
  (now also AGENTS.md): NEVER filter compile output when you
  changed the file; when tests contradict a correct-looking
  implementation, prove the ARTIFACT is fresh (=ls -la= the
  =.el=/.elc= pair, grep the =.elc= for a symbol only the new
  source defines); and find unbalance with a =syntax-ppss=
  depth-walk (per-line end depth printed) instead of hand-counting
  closers — hand-counting was wrong twice.
- Daemon gotcha (Emacs 31.1): RE-loading a =.el= source whose
  path was loaded before fails with "End of file during parsing:
  #<killed buffer>" (the reader's buffer dies mid-read).  Fresh
  paths load fine, and explicit =.elc= loads fine — verify with
  =(load ".../file.elc")= and fresh probe paths, and check
  =(documentation 'fn)= for a marker word instead of trusting
  that the load happened.
- Verified live on the Emacs 31 daemon, PR 795 buffer:
  =_marts.yml= went from 0 to 24 overlays (26 function-name-face
  =name:= values buffer-wide, up from 0), SQL files paint 21/20/
  10/31 overlays as before, the jinja macro file degrades to 1,
  and 0 warning-red overlays because this PR has no cross/comma
  join (the red face is covered by the sql-dbt-jinja test).
  Batch: 93/93 green, including the new
  =yaml-mid-block-fragment-faces= regression replicating the real
  fragment shape.
* Phase 11 — Own the diff rendering (stop breaking on magit upgrades) :STATUS:done:

Answer to "is it sustainable that we break anytime magit
upgrades?": no.  What breaks us, and the way out.

Current coupling to magit-diff (all of it internal API):

- =:override= advices on =magit-diff-insert-file-section= and
  =magit-diff-wash-hunk=, active only around a render.  Their
  contract (arguments, which lines they consume, what they
  return) is private and changed across 4.x: the paint helper
  was renamed/removed, =:washer= became a lazy-body inserter,
  and the return value of the file override drives the
  =magit-wash-sequence= loop (the raw-text bug above).
- =magit-section-paint= (feature-detected today), the
  visibility cache + =magit-section-ident-value= methods
  (supported extension points, fine to keep).

What is actually stable: the =magit-insert-section= macro,
=magit-section= classes, =magit-section-hide/show=, and the
whole section UX (TAB, navigation, ident/cache).  All of that
is magit-SECTION, not magit-DIFF.

Design sketch:

- Stop advising magit-diff entirely.  Run OUR OWN wash loop
  over the inserted raw diff: a =code-review-wash-diff=
  (parse =diff --git=, extended headers, call our file
  inserter) and a =code-review-wash-hunk= — both are already
  90% copies living inside the overrides; move them to owned
  names with owned contracts.  An upstream refactor then
  cannot change behavior under us: we read plain diff text,
  which git has kept stable for decades.
- Paint with our own ~10-line painter (per line: first char
  selects =magit-diff-added/removed/context= face).  When
  =magit-section-paint= is available, call it on top for the
  extras (whitespace, tab painting); otherwise our base pass
  already guarantees red/green.
- Keep using magit-section for sections, keymaps, visibility,
  =magit-section-ident-value= methods.
- Optionally upstream: ask magit for a supported hunk/body
  extension point so comment interleaving does not need a
  copy.  Until then the copy is the price of stability.

Cost: we stop inheriting upstream wash improvements
automatically.  Benefit: renders stop being hostage to magit
release notes.

Steps (each independently shippable, in this order so every
step stays verifiable):

**11a — dead code and shrink (STATUS:done, 2026-09-18).**  Full-package
survey (2026-09-18) found ~150 lines of dead code and ~250
lines of duplication:

- Dead, deleted: 12 orphaned db helpers
  (=code-review-db-get-path=, =-get-buffer-paths=,
  =-get-curr-head-pos=, =-get-pr-alist=,
  =-get-comment-written-pos=, =--pullreq-title-update=,
  =--pullreq-description=, =--pullreq-description-update=,
  =--head-pos=, =--curr-path-comment-count-update=,
  =--curr-path-comment-written-update=,
  =--comment-already-written?=), the never-wired
  =code-review--maybe-visit-worktree= advice wrapper, the
  unused =code-review-comment-face=, and the legacy
  =code-review-jump-to-code-review-for-a-comment-and-back=
  (hardcoded the pre-per-PR buffer name, broken and uncalled).
- =code-review-jump-to-gh= kept but fixed (same
  hardcoded-name bug; also =secure-hash= instead of a
  =shasum= shell round-trip, and the file path is taken from
  the file section instead of assuming a hunk value alist).
- =code-review-kill-pr-url= was dead by reference but useful:
  bound it in the transient menu instead of deleting.
- Survey method worth reusing: extract all defined symbols,
  count word occurrences across the package (autoloads
  included); zero outside the definition means dead.  No
  dangling calls exist (reverse scan: every called
  =code-review-*=-prefixed symbol is defined).

**11a2 — test infrastructure (STATUS:done, 2026-09-18).**  Converted
the buttercup suite to plain ERT and dropped cask from the loop
entirely:

- =make compile= / =make test= / =make deps= / =make clean= in a new
  Makefile (batch =emacs -Q=, =PKG_DIR= points at package.el
  installs); =test/run-tests.el= is the batch ERT runner; CI
  =test_suite= job now uses =make deps && make test= (no
  setup-python/setup-cask).
- =test/code-review-test-helpers.el=: DB tests run inside
  =code-review-test--with-db=, which swaps the closql singleton onto
  a fresh =/tmp= sqlite file (close connection, forget singleton,
  reset =code-review-db--pullreq-id=).  Without it the old suite
  wrote to the user's real DB: the old =:var= bindings referenced
  pre-rename variable names that no longer exist.
- Two bugs surfaced by the conversion, fixed:
  - sample PR objects must be constructed *per test* —
    closql specializes =eieio-oset= so every =oset= writes through
    to the database; a shared =defconst= object accumulated an id
    and a stale database pointer across tests.
  - =code-review-section--hide-if-hidden= now guards against the
    buffer's root section (a standalone =code-review-section-insert-*
    call in a temp buffer makes its own section the root, and hiding
    the root is a =user-error=).  Live render unaffected.
- =AGENTS.md= added: layout, build/test commands, the
  verification protocol (recompile, purge =code-review-*.eln=,
  daemon reload, fresh-render check) and the magit-wash gotchas.

**11b — own the wash (STATUS:done, 2026-09-18).**  The two
=:override= advices and the paint compat wrapper are gone; the
render runs entirely through owned washers over plain diff text:

- =code-review-wash-diff=: owned copy of magit's file-block
  washer — parses =diff --git=/--cc= and the extended headers
  (status, rename, binary) from raw text, delegates to the file
  inserter.  Only handles the plain-diff branch (API diffs never
  carry submodule/unmerged/conflict blocks); terminates the
  header scan at =^@@=/=^diff --=/=^Submodule=.
- =code-review-wash-insert-file-section=: the former advice
  override under its owned name, called directly (no dispatch
  through magit).  Keeps classification, collapse, comment
  bookkeeping and the trailing "missing comments" pass; still
  returns the section (=magit-wash-sequence= contract).
- =code-review-wash-hunk=: the former hunk override under its
  owned name; interleaves position- and side/line-keyed comments
  into the hunk body.
- =code-review-wash--paint-hunk=: our own base pass (first char
  of each line selects =magit-diff-added/removed/context=),
  which guarantees red/green on every magit version; the stable
  =magit-section-paint= generic is called on top when available
  for the extras.  Replaces the three-way compat wrapper.
- =code-review-wash--decode-git-path=: =magit-decode-git-path=
  behind an =fboundp= guard (identity fallback).
- Driver: =(magit-wash-sequence #'code-review-wash-diff)=; the
  =unwind-protect= advice add/remove pair is deleted;
  =magit-diff-wash-diffs-hook= still runs (public hook).
- Bonus fix discovered while restoring a render context:
  =code-review-db-all-unfinished= referenced the pre-rename
  free variable =code-review-db-connection= (void-variable on
  every call); the stale close block is deleted — connections
  are owned by the closql singleton.
- What we still use from magit: =magit-insert-section=, section
  classes, =magit-wash-sequence=, =magit-section-hide/show=,
  the visibility cache + =magit-section-ident-value= methods,
  and =magit-section-paint= as an optional extra — all
  magit-SECTION (stable).  Nothing in magit-DIFF is called
  except the public =magit-diff-wash-diffs-hook= run.
- Verified: 11 files / 28 hunks / 1422 painted / 0 unpainted /
  0 raw headers, bots 40/30 hidden, 12 folded; re-render
  identical; user's live litellm buffer healthy; ERT 35/35.
- POST-MORTEM (2026-09-21, three days after "done"): the owned
  entry regex was a mis-transcription of magit's -- the optional
  backreference group =\\(?:\\(?2:.+?\\) \\2\\)?= was dropped, so
  the pattern required both paths on the =diff --git= line to be
  IDENTICAL.  No standard =a/X b/X= line matches that: the
  washer returned nil on the very first block,
  =magit-wash-sequence= stopped, and the entire diff landed as
  raw, uncolored text.  Renames made it obvious (the first raw
  block in the user's writer-data-platform#762 review), but
  EVERY file was affected on any fresh render.  The "verified 0
  raw headers" claim above was fooled by stale in-memory/native
  code in the daemon (the pre-11b adviced washers were still
  serving renders).  Fix: restore magit's optional group;
  regression test =code-review-section-test/wash-diff-rename-block=
  (zero raw lines, rename file section present, painted faces)
  now guards it.  Lesson: when transcribing magit internals,
  copy patterns EXACTLY and prove the copy against a real
  =diff --git= line in a fresh batch emacs -- a compiling regex
  is not a matching regex.





**11c — split section.el (STATUS:done, 2026-09-18;
correction 2026-09-19).**  Split along the seams: new
=code-review-diff.el= (14 defs: the phase-3 classification
engine + two defcustoms, pure functions on raw diff text;
requires db + repo), new
=code-review-reactions.el= (18 defs: reaction classes and the
toggle machinery; requires db + interfaces), and 22
interactive/navigation commands moved into
=code-review-actions.el= (with =(require 'code-review-repo)=
and declare-functions for section helpers).
=code-review-section.el= went 3,198 -> 2,394 lines and now
=(require '= code-review-diff/reactions).  Pure moves, no logic
changes.  Makefile =compile= was silently skipping the first
directory entry (the =cdr= bug: =actions.el= was never
byte-compiled) and trying to compile autoloads/pkg; fixed with
a plain =member= exclusion list (do not put regexps through
make/shell escaping — "Unmatched (" is what you get).
Verified: all 13 files load, classification tag renders from
the new home, fresh render 11 files / 28 hunks / 1,481 painted
/ 0 unpainted, bots 45/34 hidden, 13 folded; re-render
identical; live litellm buffer healthy; ERT 35/35.

CORRECTION (2026-09-19): the verification above was partly a
LIE.  =code-review-diff.el= on disk had only the two
defcustoms: the 12 classification functions (and
=code-review--maybe-reorder-diff=) were extracted out of
section.el but never landed in diff.el.  Three things hid it:
byte-compile was silenced by the =declare-function= forms in
section.el, ERT never called them, and the daemon kept the
pre-split definitions in memory (defuns survive =load=).
The stale definitions made every live render look perfect.
Symptom that exposed it: in a local diff review the LAST
hunk's lines lost their faces —
=code-review-utils--clean-diff-prefixes= ends with
=(string-trim res)= which eats the diff's trailing newline;
=code-review--maybe-reorder-diff= joined blocks with
=(mapconcat #'cdr kept "")=, so when reordering moved a file
after the (trimmed) raw-last block, its ="diff --git"= header
glued onto the dangling hunk line and the wash could not
recognize it (raw text rendered inside the previous hunk).
Both fixed: =clean-diff-prefixes= now trims only LEADING
whitespace, and the reorder join terminates every block with
a newline.  Functions restored from
=/tmp/code-review-section.el.bak-11c= (3,198-line pre-split
backup).  New regression tests
(=test/code-review-diff-test.el=: split, classify tags,
reorder-never-glues) force a FRESH batch emacs to load and
run the engine, which is the only check that cannot be
fooled by daemon staleness.  38/38 pass.  Lesson: after any
extraction, prove the new file's functions exist in a fresh
batch process, not in the long-lived daemon.

**11d — dedup (STATUS:done, 2026-09-18).**  The reaction
families collapsed into one engine in
=code-review-reactions.el=:
=code-review-reaction--update-node= (upsert/delete one
reaction on one comment alist, by =databaseId=),
=--update-flat= (conversation: comments + reviews nodes),
=--update-nested= (code-comment: reviews -> comments, syncs
=raw-comments= too), =--update-description= (PR-level
reactions), and =--update-context= (pcase dispatcher that
persists via =code-review-db-update=).  Eight fns deleted
(description/conversation/code-comment add + delete and the
two =--add-or-delete= drivers); the three
=*-reaction-at-point= commands are 3-line wrappers over
=code-review-reaction--toggle-in-context= (which now also
restores the cursor in the description context).  Same pass:
merge trio in actions.el -> =code-review-merge--with=
(strategy arg) + three autoloaded wrappers;
=code-review-utils--unknown-errback= and
=code-review-utils--not-supported= in utils (gitlab/bitbucket
public fns now delegate, call sites untouched);
=code-review-user-visit-at-remote= + two defaliases;
=comment-insert-lines= local/reply defmethods share
=code-review-comment--insert-you-lines=, driven by two new
class slots =render-heading= and =render-extra-newline?= on
the local/reply defclasses.  One intentional behavior change:
the description reaction toggle now records the cursor
position like the other two contexts.
Verified: engine add/delete upserts checked on synthetic
alists; fresh render 11 files / 28 hunks / 1,481 painted /
0 unpainted, re-render identical; live litellm buffer healthy;
ERT 35/35; no source references to the deleted fns remain;
stale daemon bindings fmakunbound.

Not worth touching: =internal-build= per forge (bodies differ
in real per-forge mapping), comment.el, utils.el,
interfaces.el, repo.el (all clean).

* Phase 12 — Local diff review (review your own changes)      :STATUS:done:

Review the working tree as if it were a PR: fold docs/noise, see
classification tags, difftastic drill-down, xref, the works —
before committing.  Everything phase 11 made forge-agnostic
(owned wash, classification, folding) works on local diff text
for free.

Design decisions (2026-09-18, user):

- =code-review-review-local-diff=: no prefix =3D =git diff HEAD=
  (staged + unstaged), one prefix = =git diff --cached=, two
  prefixes = prompt for a ref.  New file =code-review-local.el=.
- The local "PR" is a real DB row: class =code-review-local-diff=
  (subclass of =code-review-db-pullreq=, NO extra slots — the
  closql schema is fixed; the git diff args ride the existing
  =base-ref-name= column, the worktree comes from
  =magit-toplevel= at render time).  =state= = "LOCAL" is the
  marker everywhere.  Rows are deleted on the next local review
  start (=code-review-local--cleanup-rows=); they are never
  =saved=3D= so they never appear in unfinished reviews.
- Render path reuses the real machinery: local =cl-defmethods=3D
  for =code-review-diff-deferred= (runs git diff),
  =code-review-infos-deferred= (empty),
  =code-review--auth-token-set?= (nil) and
  =code-review--internal-build= (stores raw-diff, triggers
  hooks).  So =G= (full reload) re-runs =git diff= for free.
- Header shows the title "Local changes (<args>)" and state
  LOCAL; info-driven headers render empty when raw-infos is nil.
- Difftastic: =code-review-difftastic--range= returns the stored
  diff args for local rows (=HEAD=, =--cached= or the ref), so
  =D= and =C-u D= work unchanged.
- Comments and reactions are READ-ONLY (user decision): every
  comment/reaction entry point and every forge-mutating command
  user-errors early ("local diff reviews are read-only").
  Guards: =code-review--submit= (covers approve/comments/request
  changes/LGTM/only-replies), the single-comment and setter
  commands, close-pr, merge, save-unfinished, delete-comment,
  threads-toggle-resolved, comment-add-or-edit,
  comment-code-suggestion, the reaction toggles, kill-pr-url.
- Constraint carried forward: any Phase 10 (tree-sitter) or
  Phase 5 (duplicate/dead code) machinery MUST apply to local
  reviews as well — both operate on the diff text/sections, so
  they inherit this for free, but verify with a local buffer.

**Implementation (STATUS:done, 2026-09-19).**  =code-review-local.el=
as designed; =code-review-db-local-pr-p= in db.el;
=code-review-pr-buffer-name= and =code-review--trigger-hooks= local
branches in section.el; ~25 read-only/forge guards (one line after
the interactive form of every mutating command in actions.el,
comment.el, reactions.el, code-review.el).

Verified live on this repo's own diff (7 files / 36 hunks / 461
painted / 0 unpainted): render, re-render via =G= (re-runs git
diff), difftastic range, guards 5/5, ERT 35/35, litellm buffer
healthy.

Hard-won gotchas (all bit during implementation):

- =magit-git-string= returns the FIRST LINE ONLY.  Diff text
  needs =magit-git-output=.  (Symptom: 44-char "diff".)
- =oset= on the closql pullreq is memory-only: these classes
  have no per-slot =:closql-table= props, so nothing writes
  through.  Persistence is an explicit =(closql-insert db obj t)=
  (exactly what =code-review-db--pullreq-raw-diff-update= always
  did).  Symptom: state nil on refetch, generic buffer name.
- The pullreq schema is FIXED at 32 columns and matches the
  forge classes, which each define exactly one extra =callback=
  slot; a subclass with a different slot count fails insert
  with "32 columns but 31 values".
- The deferred render runs in a TIMER: ambient
  =default-directory= is arbitrary (the repo root is stored in
  the =host= column) and =code-review-db--pullreq-id= may have
  been switched to another buffer's PR by the
  =post-command-hook= sync.  =code-review--internal-build= now
  re-asserts =(setq code-review-db--pullreq-id (oref obj id))=
  first — in the local method AND in the three forge methods
  (that race predates phase 12).
- =(require 'foo)= is a no-op once the feature is loaded:
  reloading via require keeps STALE definitions (including
  native-jit subrs) in the daemon.  Reload every touched file
  with =load=, and add new files to the verify probe's file
  list.
- The ref header inserts =head-ref-name= unconditionally: local
  rows set it to "working tree".

**Extension (STATUS:done, 2026-09-25): review the magit buffer's
diff.**  Running =code-review-review-local-diff= with no prefix
inside a magit buffer now reviews WHAT THAT BUFFER SHOWS instead
of the working tree: the shown commit (in
=magit-revision-mode=, diffed against its first parent,
="REV^..REV"=; ="REV^!="= for a root commit, which has no parent
to diff against — ="REV^..REV"= fails there with exit 128,
verified with real git), or the shown range (in
=magit-diff-mode= with =magit-buffer-diff-range=).  Outside
magit, nothing changes: working tree, staged (=C-u=), or a
prompted ref (=C-u C-u=) as before.  Title =Commit <short>
(<subject>)= / =Diff <range>= rides the existing =title= column;
the diff args ride =base-ref-name= (no schema change), and the
G (full reload) path re-fetches them through
=code-review-local--diff-text= for free.

The review buffer name carries the reviewed diff as a hint
(=code-review-local--buffer-name-hint= in section.el, placed
there to avoid a cross-file dependency):
=*Code Review: local: <repo> @ <short-sha>*= for a commit (7-char
short form when the args are full SHAs; the raw rev, e.g.
"HEAD", otherwise), =@ <range>= for a range, and NO hint for the
classic args (=nil=/"HEAD"/"--cached"/a prompted ref) — so
existing buffer names are unchanged and the hint doubles as the
kill target.  No-prefix semantics changed only for the
magit-buffer case; all classic invocations keep their old
meaning.

Verified: 7 new ERT tests (119 total, 115 passed + 4 skipped)
including an end-to-end that runs the real entry in a temp
=magit-revision-mode= buffer and pumps the deferred chain until
the row's =raw-diff= lands and =deferred:queue= drains; live in
the daemon on this repo's HEAD commit (9 files / 18 hunks /
42 sections, buffer =*Code Review: local: code-review @ HEAD*=)
plus a =G= full-reload stability check (identical counts — the
re-fetch uses the stored ="HEAD^..HEAD"=).

**Extension (STATUS:done, 2026-09-25, later that day): the
interactive no-prefix bug, and magit-log support.**  The user
reported the feature "kept opening the files I have staged"
from a =magit-revision= buffer: =interactive "p"= passes ARG 1 —
NOT nil — for no prefix, so the entry's =(not arg)= guard was
never true interactively and every real invocation fell back to
=git diff HEAD= — only direct =(code-review-review-local-diff)=
calls (ARG nil, as in the ERT e2e) exercised the magit branch.
The guard is =(member arg '(nil 1))= now, and the e2e calls the
entry with ARG 1 (the value the command loop passes).

BONUS (user request): in a =magit-log-mode= buffer the entry now
reviews the COMMIT AT POINT, and with a region marked over
commit headings the COMBINED diff of ALL the commits in the
region — from the OLDEST selected commit's parent to the NEWEST
(="OLD^..NEW"=; a region of one commit is just that commit's
diff).  That is "the diff of the commits in the region", NOT
magit's own =d=-on-region convention ="OLD..NEW"= (endpoint
trees), which EXCLUDES the oldest commit's changes — verified
with real git in a scratch repo.  When the oldest selected
commit is the repository's ROOT commit (no parent,
="ROOT^..NEW"= fails with exit 128) the range starts at the git
EMPTY TREE instead
(=code-review-local--empty-tree=, =4b825dc...=, the same sha in
every repository, valid without the object existing): every
selected commit's changes land in the diff.  Titles:
=Commit <short> (<subject>)= / =Commits <short-old>..<short-new>=.
Region detection rides =magit-region-values '(commit branch) t=
(magit's own region machinery, newest-first) and the mark is
deactivated on use, like magit's dwim.  The buffer-name hint
grew a generic range case: ="OLD^..NEW"= shows as
=@ <short-old>..<short-new>= (each full-sha side shortened, the
first-parent ="^"= dropped; the empty-tree side shortens like
any sha: =@ 4b825dc..<short>=).

Verified: 122 ERT tests (118 passed + 4 skipped) — log
commit-at-point, log region (combined diff of the two newest:
both =+def= additions in the diff, the root's absent), log
region including the root (empty-tree range: the root's
addition IS in the diff) — plus live in the daemon: commit at
point (=f256edc^..f256edc=, 9 file sections, exactly the
commit's files) and region over the two newest commits
(=afaf5ce^..f256edc=, 12 file sections, both commits' files).

Gotchas:

- =interactive "p"= passes ARG 1 — NOT nil — for no prefix: any
  no-prefix behavior keyed on =(not arg)= silently never runs
  interactively (only direct calls with no argument exercise
  it).  The e2e must call the command with 1, not with nothing,
  or it tests a path real keypresses never take.
- magit's log machinery chokes on split =-n= args in batch:
  =(magit-log-head (list "-n" "3"))= dies inside
  =magit-log-get-commit-limit= with =stringp nil= (the ="-n"=
  form matches magit's ="^-n\\([0-9]+\\)?$"= with an EMPTY
  group; =(string-to-number nil)= explodes).  Pass the merged
  form =(list "-n3")= — what the transient actually produces.
- magit-log commit sections carry SHORT shas as values (not the
  40-char form): assert =7,40= hex, and expect short shas in
  the stored diff args and the buffer-name hint.
- "the diff of the commits in the region" is NOT magit's
  =d=-on-region convention: magit diffs the endpoint trees
  (="OLD..NEW"=), which excludes the oldest commit's own
  changes.  Including them needs ="OLD^..NEW"= — and a
  root-commit oldest needs the EMPTY TREE trick (probe before
  shipping range semantics).

- ERT's =(equal RESULT "*...@ HEAD*")= does NOT glob: =.*= in a
  string is a literal dot-star, the assertion fails on
  ="...@ HEAD*"= vs ="...@ Yd0NMC..."=.  Match names with
  =string-match-p= and a real regexp.
- The deferred render chain is ASYNC in batch too
  (=deferred:parallel= posts callbacks into =deferred:queue=,
  consumed by timer ticks): an ERT e2e must poll =(sit-for
  0.05)= until the row's =raw-diff= is non-nil AND
  =deferred:queue= is nil, with a deadline — the DB write
  (=closql-insert= in =--internal-build=) landing means the test
  can assert before the test's db reset, and a drained queue
  means no async work survives the reset.
- =magit 4.x buffer names have no leading star: a revision
  buffer is named "magit: commit <rev>", not "*magit:
  commit*" — =(get-buffer "*magit: commit*")= returns nil.  Find
  them by =(buffer-name b)= prefix "magit:" or by
  =derived-mode-p 'magit-revision-mode=.
- A surplus closer AFTER a complete top-level form does not
  stop that form from being read and evaluated: the reader
  closes the form early, evals it (so its side effects land),
  and the =load= only signals on the NEXT top-level read.  A
  probe can therefore report correct results from an unbalanced
  file — always check-parens / depth-walk probe scripts
  regardless.

* Phase 13 — Read tracking: mark files/hunks as reviewed      :STATUS:todo:

Problem: folding a hunk once you have read it is the natural
review workflow, but magit's visibility state lives only in the
session: re-renders, restarts and =G= keep the folding only by
accident, and nothing survives between review sessions.  There
is no memory of what has already been reviewed.

Goal: =v= marks the file (or hunk) at point as read; the state
persists, syncs with GitHub's "Viewed files" feature, and
un-marks itself when the diff changes underneath it — exactly
like the GitHub web UI, where a viewed file flips back to
unviewed when a new contributor commit touches it.

Design:

- SOURCE OF TRUTH for forge PRs: GitHub itself.  The web UI's
  "Viewed" checkbox is file-level and already has the wanted
  reset semantics (new commits to the file mark it unviewed
  again).  So do not reimplement staleness logic for GitHub:
  mirror it.
  - Mutations: GraphQL =markFileAsViewed= /
    =unmarkFileAsViewed= (input: =pullRequestId=, =path=).
  - Query side: fetch the per-file viewed state with the rest
    of the infos and seed the local DB from it (verify the
    exact field name in the GraphQL schema during
    implementation; the REST files endpoint does not carry it).
  - Marking is optimistic: update the local DB, re-render, and
    push the mutation asynchronously; an errback reverts the
    local state and messages the user.
- LOCAL STATE (needed for hunks, for GitLab/Bitbucket which
  lack the same API, and for local diff reviews): a new closql
  table (e.g. =reviewed=: pullreq, path, hunk-key, updated-at).
  - Hunk granularity keyed by =file + hunk header=
    (=@@ -a,b +c,d @@=), which survives most re-renders; a
    changed header means the diff changed =3D read state
    expires — that reproduces GitHub's reset semantics
    locally without diffing diffs.
  - File viewed =3D all its hunks read; then the GitHub sync
    fires (for GitHub PRs).
  - Schema note: new TABLE, not new columns (the pullreq schema
    is fixed and closql insert maps the whole object); create
    it =IF NOT EXISTS= on connect so existing user DBs migrate
    transparently.
- UX:
  - =v= on a hunk section: mark that hunk read (dim + auto-fold
    on next render); =v= on a file section: mark the whole file
    read; =C-u v= unmarks.  Also works in =*Code Review:
    local...*= buffers, DB-only.
  - Read files get a =VIEWED= tag on the file heading (same
    mechanism as =DOC=/=GEN= tags) and stay folded by default;
    =code-review-toggle-focus-mode= gets a companion that hides
    them entirely.
  - "Files changed" heading reports "8/14 viewed".
  - =G= (full reload) must reconcile: GitHub state wins for
    GitHub PRs, local state fills in for the rest.
- Implementation order: (1) DB table + =v= command with local
  persistence and rendering (works everywhere, including local
  reviews); (2) GitHub sync on top (query seed + optimistic
  push); (3) GitLab/Bitbucket if their APIs allow, DB-only
  otherwise.
- Verification lessons already learned apply: batch tests in a
  FRESH emacs for the DB table and hunk-key expiry; render
  checks for the tag/folding; live litellm buffer health; and
  beware that daemon staleness lies (see phase 11c correction).

* Phase 14 — Review heat: risk-ranked reading order          :STATUS:done:

DONE (implementation notes, 2026-09-25) — with the code-compass
split the research note anticipated: the GENERAL repo-metrics
primitives live in code-compass (upstreamable to ag91), the
review-specific orchestration lives here.

- CODE-COMPASS (ag91/code-compass, local checkout, 16/16 ERT):
  - =code-compass--parse-git-log-metrics=: pure parse of a
    =git log --name-only= log (the code-maat
    =--%h--%ad--%aN= header format) into per-file plists
    =(:revisions :authors (author . count) sorted desc
    :main-dev :last-touch)=, with bot-author filtering.
  - =code-compass-file-metrics= (REPO &optional SINCE): ONE
    partial-clone-safe =git log --all --name-only=
    (=--numstat= would lazy-fetch blobs per changed file on
    =--filter=blob:none= clones; =--name-only= fetches
    nothing).  The elisp-data counterpart of the code-maat
    revisions/main-dev analyses.
  - =code-compass-file-complexity=: file-level wrapper over the
    existing indentation-stats machinery.
  - =code-compass-show-file-metrics=: quick ranked table
    command.  Fixed two PRE-EXISTING broken tests on the way
    (they called the pre-rename dashed complexity function).
- CODE-REVIEW (=code-review-history.el=, new): soft dependency
  (=require 'code-compass nil t=; no fallback reimplementation —
  degrade with a message).
  - HARVEST: =--repo-key= (worktree -> common git dir; two PRs
    of one clone share the cache), cache file
    =<common-git-dir>/code-review/history-<window>.el=, TTL
    (default 7 days), in-memory warm layer.  The git log
    EXCLUDES the review package's own fetched PR refs
    (=--exclude=refs/remotes/code-review/*= — otherwise every
    review would count as repository churn) and bot authors
    (the phase 9a regexp).
  - ASYNC (daemon rule): first render of a repo shows a
    "harvesting" message and kicks a CHILD BATCH EMACS (the
    compile-buffer pattern; full =load-path= serialized into
    =-L= flags, the defcustom values passed in the =--eval=
    form) that runs the log + parse and writes the cache; the
    sentinel re-renders the review buffer (after
    =code-review--sync-db-pullreq= — the db pullreq id is a
    global and the user may have opened another PR meanwhile).
    Failure marking prevents harvest loops.  Render afterwards
    is a hash lookup.
  - HEAT (pure, ERT): churn PERCENTILE (per-repo normalized)
    x complexity (max logical indentation of the file at the PR
    head, code-compass stats) x knowledge (authors spread >= 5
    -> x1.1; PR author in the repo pool with zero commits on
    the file -> x1.25 and a "never touched it" reason — gated:
    a login absent from the repo's authors may just commit
    under a different name, so NO claim then; bot PR authors get
    no knowledge claims).  Buckets HOT/WARM/COLD by churn
    percentile (0.9 / 0.7 defaults).  Local reviews: the repo
    itself is the worktree and the local git user is the
    author.
  - RENDER: "Review order (heuristic)" section right after the
    phase 5 Analysis section (jump buttons to the file sections
    IN THE REVIEW BUFFER via a path-based section walk), file
    heading tags via the phase 3 machinery (only on files the
    noise checks left untagged — noise wins), "order by heat"
    within the existing ordering (user rules first, noise still
    sinks — verified against the user's own 4-rule config),
    focus-mode COLD hiding behind
    =code-review-history-focus-hide-cold= (default nil).
    =code-review-history-reset= re-harvests on demand.
  - VERIFIED LIVE on the open local review of
    writer-data-platform: 12-month harvest (553KB cache),
    15 changed files ranked; top entries exactly the phase's
    use case — =_marts.yml= HOT "top 1% churn (143 revisions);
    17 authors; main dev: Laurence Bird" first,
    =mart-migrations.ts= HOT second, the new COLD files sink
    with "Andrea never touched it" reasons; HOT/COLD tags on
    the file headings; suite 113/113.
  - GOTCHAS (see AGENTS.md): =--exclude= must come AFTER the
    =log= subcommand (a global-position =--exclude= makes git
    error out and =call-process= discards stderr, so the
    harvest silently returned nothing); a compile killed by a
    command timeout leaves a STALE .elc that the next =make
    test= happily runs; =search-forward= searches LITERALLY
    (probe bug, not a render bug).

NOTE (phases 14-18, research 2026-09-24): these phases turn the
review of a large PR from scrolling in path order into working a
ranked, contextualized queue of the delicate parts.  Evidence
base: code churn x complexity is the best validated "where bugs
live" signal we have (Nagappan & Ball, ICSE 2005: relative code
churn predicts defect density; CodeScene's numbers: the
prioritized hotspots of one codebase were 1.2% of the code,
consumed 12.5% of dev effort, and held 45% of all bugs).
Just-in-time defect prediction applies the same signal family
to individual changes, down to hunks ("Just-in-time defect
prediction for software hunks", 2021), and effort-aware JIT
ranks changes by defect likelihood divided by inspection effort
— literally a review reading order.  For agent PRs specifically:
GitHub's agent-PR reviewer guide (2026-05) prescribes
scan-and-classify first, then "trace ONE critical path"; names
code-reuse blindness the highest-ROI check (phase 5 already
automates it); and observes agents carry "zero context about
your incident history" — history is exactly what we have.  An
EASE 2026 study of 33k agent PRs found 61% receive no recorded
review and 84% no observable human involvement, so
attention-preserving tooling has headroom.  DESIGN STANCE:
everything here is deterministic git / treesitter / grep; the
only AI is an optional garnish under phase 16, OFF by default.
Per the phase 12 constraint, all five phases must work in local
diff reviews too.  The sources behind these claims are collected
in the "References" section at the end of this file.

PRIORITIZATION (long-term review effectiveness, /10; judgment
weighted for durable issues-found-per-unit-attention):
14 reading order 8.7 > 15 hunk delicacy 8.4 > 16 dossier 8.3 >
17 test impact 7.9 > 18 coupling 7.6 > 19 cross-repo 7.5.
Build order: 14 then 15 as ONE pipeline (shared harvest below,
file level first), 16 next (nearly free once 14/15 exist), then
17 and 18 in either order; 19 last, once the harvest layer
exists (it reuses the harvest SHAPE, not its data).

SHARED HARVEST LAYER (new =code-review-history.el=, used by 14,
16 and 18): ONE async =git log --name-only --since=<window>= per
repo (default 12 months) parsed in pure elisp into per-file
facts — revision count, distinct authors, main dev, last-touch
date — plus a co-change co-occurrence matrix for phase 18.
Cached per repo with a TTL (default 7 days): shared across every
PR of the same repo, render-time is a hash lookup, and the
harvest NEVER runs during the wash (daemon rule, AGENTS.md).
PARTIAL CLONE GOTCHA: phase 1 clones are =--filter=blob:none=;
=git log --name-only= compares tree entries by blob OID and
fetches NOTHING, while =git log --numstat= needs blob CONTENTS
and lazy-fetches over the network per changed file per commit.
So the harvest counts REVISIONS, not line churn — which is
exactly CodeScene's hotspot formula (revisions x complexity)
anyway.  Alternative backend: code-compass already runs
code-maat =revisions= / =main-dev= / =fragmentation=
asynchronously with a CSV cache (jar auto-downloaded); when
present, read it (upstream a tiny =code-compass-get-file-metrics=
accessor to ag91) instead of reimplementing.

Problem: in a 40-file PR the reviewer meets the delicate file
at position 37.  History knows better.

Design:

- HEAT SCORE per changed file: recency-windowed revision count
  (per-repo normalized, so an active repo does not saturate the
  scale) x complexity (cloc via code-compass, or the phase 10
  treesitter branch/function counts) x knowledge factors
  (distinct authors; the PR author's prior commits on the file;
  distance from the file's main dev).  For bot-authored PRs the
  author factor reads as "how much context does the file's main
  dev have that neither the bot nor you do".
- Render: a "Review order (heuristic)" section in the phase 5
  Analysis slot — ranked files, jump buttons, one-line reasons
  ("top 3% churn x high complexity; main dev: mrossi; PR author
  never touched it").  File heading tags =[HOT]= / =[WARM]= /
  =[COLD]= via the phase 3 plist =:tag= machinery; an "order by
  heat" mode on top of the existing ordering (noise still sinks
  to the bottom); a focus-mode refinement that hides COLD too.
- Compute contract: harvest async (async.el or a compile
  buffer), NEVER in the render; first review of a repo shows a
  "harvesting history" message, afterwards hash lookups only.

Use case — what this catches in review:

- The agent edited the repo's #1 hotspot while you would have
  started with the plumbing files: regressions in high-traffic,
  defect-prone code get your attention FIRST, while you still
  have it.
- "Nobody really owns this": high author spread on a hot file is
  where plausible-looking AI changes are most likely wrong; you
  slow down exactly there.
- Files whose main dev is neither you nor the PR author: you
  know whom to loop in BEFORE approving, not after the incident.
- Honest triage of the rest: COLD files get skimmed guilt-free
  instead of dutifully read and forgotten.

Tuning/caveats: heat punishes actively-developed healthy modules
(keep scores normalized and windowed); bot commits in history
(renovate, dependabot, ...) count as churn — optionally filter
with =code-review-bot-author-regexp=; expect one tuning round
(phase 5 scalafmt precedent).

* Phase 15 — Hunk delicacy score and delicate-hunk navigation :STATUS:todo:

Problem: file ranking finds the room; review happens at hunk
granularity.  This phase is hunk-level just-in-time defect
prediction, and it mechanizes GitHub's "trace one critical path"
into one keypress.

Design (extends =code-review-analysis.el=: it already extracts
def names, batched-greps references, and caches per (PR id,
diff md5)):

- Signals per hunk, each bounded and cached:
  - BLAST RADIUS: call-site count in the worktree for the defs
    the hunk touches (ONE batched =git grep= for all defs, the
    phase 5 pattern; never one grep per def).
  - LINE AGE / OWNERSHIP: one cached =git blame --porcelain= per
    changed file; median age and distinct authors of the context
    lines being modified.  Old stable lines being edited is the
    classic delicacy signal.
  - COMPLEXITY DELTA: phase 10 treesitter branch nodes added vs
    removed in the enclosing function.
  - DEAD-ON-ARRIVAL: zero-reference additions (phase 5 already
    computes them) feed the score.
  - TEST COVERAGE of the touched defs (feeds phase 17).
- Render: a compact reason badge on the hunk heading ("risk: 40
  callers; lines 3y old; +18 branches"); =C-c C-d= and a
  transient entry cycle the top-K delicate hunks (sibling of
  =C-c C-n= / =C-c C-p=); the Analysis section gains a ranked
  "Delicate hunks" jump list.
- PARTIAL CLONE GOTCHA: =git blame= lazy-fetches blobs for the
  file's history — cache per file, message on first use, never a
  silent render-time fetch.
- PHASE 13 SYNERGY: read tracking should report WEIGHTED
  coverage ("5 of 7 high-risk hunks read"), not raw file counts;
  design the hunk key now so phase 13 can reuse the score.

Use case — what this catches in review:

- The "trivial" one-line change to a helper with 40 call sites —
  the classic AI regression vector; the badge makes the blast
  radius impossible to skim past.
- Modifications of lines stable for 3 years: stable code is
  stable for a reason the agent does not know; blame age puts a
  number on it.
- Complexity bombs: a def going from 4 to 25 branches stays
  visible even inside a 2,000-line hunk.
- New code that nothing references or nothing tests: surfaced as
  score ingredients instead of separate findings.

Caveats: blame signals are noisy on generated/reformatted code;
expect one tuning round.

* Phase 16 — Hunk dossier: on-demand change context           :STATUS:todo:

Problem: ranking says WHERE the delicate spots are; it does not
say WHY they are delicate, so you cannot judge whether the
implementation choice makes sense.  The agent never had the
incident history; the dossier puts it one keypress away.

Design:

- =C-c C-h= on a hunk inserts a collapsible "Context" section
  (or a dedicated buffer):
  - HISTORY OF EXACTLY THOSE LINES: =git log -L
    <start>,<end>:<file>= capped to N commits, with messages and
    dates ("these lines last touched 6 months ago by mrossi,
    'fix race in session teardown'").
  - Blame authors of the context lines.
  - ALL CALL SITES of the touched defs, as jump buttons into
    the worktree (composes with phase 2 xref and the phase 8
    ediff plans).
  - REFERENCING TESTS for the touched defs.
  - The file's heat and coupling facts (phases 14/18 data).
- On-demand only: zero render cost; cached per (PR id, diff md5,
  hunk).  =git log -L= lazy-fetches blobs of that ONE file on
  partial clones — message the first time.
- OPTIONAL AI GARNISH (user is AI-skeptical; OFF by default):
  =C-u C-c C-h= may pipe the DOSSIER — not the PR — to an LLM
  for a three-sentence "story of these lines", rendered in a
  distinct face marked "machine summary", cached per (PR, diff
  md5, hunk), NEVER a verdict, and the buffer stays fully useful
  with the feature off.  Non-deterministic in phrasing only:
  every claim must be traceable to a dossier entry.  Evidence
  for the restraint: Alami & Ernst 2025 (validating AI
  suggestions adds cognitive load); Tufano et al. 2024
  (automated review fails exactly where broad context is needed
  — i.e. at the delicate parts).  The direction matters: an AI
  that summarizes OUR deterministic facts is the opposite of an
  AI reviewer; the facts are ours, only the prose is borrowed.

Use case — what this catches in review:

- Rewrites that silently drop an edge case a past commit added
  ("fix empty payload crash"): the dossier shows the line's
  REASON FOR EXISTING before you judge the rewrite.
- Changes that fight the code's direction: the last five commits
  on those lines were simplifications, and the PR adds a special
  case back.
- Contract mismatches: the call-site list shows how callers
  actually use the def the hunk rewrites (defaults,
  preconditions the agent guessed wrong).
- "Why is this here at all": old lines with lore stop being
  rubber-stampable; plausible rewrites get challenged.

* Phase 17 — Test-impact verification and CI-gaming detector  :STATUS:todo:

Problem: when agents fail CI they have an obvious path to green
(remove tests, add skip markers, lower thresholds, gate steps).
GitHub's agent-PR guide lists CI weakening as red flag #1 and
asks for tests that FAIL on pre-change behavior; both are
mechanically checkable in the worktree phase 1 gives us.

Design:

- TEST MAPPING: changed defs (phase 5/15 name extraction) ->
  test files referencing them (grep, reusing
  =code-review-analysis-dead-test-name-regexp= and the phase 10
  =code-review-hunkhighlight-test-path-regexp= machinery).
  Per-hunk =[NO-TEST]= tag when a touched def has zero
  referencing tests.
- =T= on a file/hunk runs EXACTLY the mapped subset in the
  worktree via compile / project.el; a per-project test command
  defcustom (=code-review-test-command-alist=) is the one real
  setup cost.  For forge PRs, optionally run the same subset at
  =refs/remotes/code-review/N/base= as well: a test that passes
  at BASE and at HEAD does not cover the change — GitHub's
  "require a test that fails on the pre-change behavior", made
  mechanical.
- CI-GAMING DETECTOR (pure regexps, cheap enough for the
  render): skip markers added (=test.skip=, =xit(=,
  =@pytest.mark.skip=, =|| true= in test commands), coverage
  thresholds lowered, workflow steps newly gated or removed.
  Hard tag on the offending file plus a hoisted entry in the
  Analysis section.
- LOCAL REVIEWS: phase 12 buffers have no CI at all — this is
  their only automated verification; =T= works unchanged there.

Use case — what this catches in review:

- CI gaming: tests removed, skipped or thresholds weakened to
  get the pipeline green — the top agent-PR failure mode,
  surfaced as a hard tag instead of a lucky catch.
- Untested delicate changes: the hunk that matters has no
  covering test; you demand one BEFORE approving instead of
  discovering it post-merge.
- Fake-fix tests: the new test passes on the pre-change code too
  (run at base), so it asserts nothing about the fix.
- Your own pre-commit work (local reviews): which of your
  changes lost coverage, without a CI round trip.

Caveat: the mapping is a heuristic (dynamic dispatch,
reflection, build-convention entry points are missed) — label
it as such, phase 5 style.

* Phase 18 — Change-coupling completeness cross-check          :STATUS:todo:

Problem: the repo's own history knows which files change
TOGETHER; an agent's patch is often narrower than the
historical change set — an incomplete refactor.  Evidence:
change coupling correlates with defects (D'Ambros & Lanza
2009; Wiese et al. 2015; Software Maintenance and Evolution
2017), and CodeScene treats source+test co-change as POSITIVE
coupling (tests kept in sync), which gives the star check
below.

Design:

- For each changed file, its co-change peers from the phase 14
  harvest co-occurrence matrix (degree >= threshold, default
  30% as in code-maat; cap changeset size like code-maat's
  =--max-changeset-size=).  code-maat =coupling= via
  code-compass (already async + cached) is the alternative
  backend.
- Checks, rendered in the Analysis section plus per-file
  context with jump buttons:
  - Coupled peer NOT in the PR: "changes together with X (58%)
    — X untouched: is the refactor complete?"
  - POSITIVE COUPLING broken: source file changed while its
    coupled TEST file did not ("tests historically evolve with
    this file; this PR left them behind").
- Cache per repo with the same harvest TTL; render-time is a
  lookup.

Use case — what this catches in review:

- Incomplete refactors: the agent patched the source but not
  the sibling that historically always changes with it (config,
  migration, proto, serializer).
- Stale coupled tests: source and test co-changed 40 times
  historically; this PR changes only the source.
- Convention pairs no doc records (route + controller, schema +
  mapper): the repo's own history is the specification.

Caveats: monorepos and renames are noisy (code-maat's git2 log
format and =--no-renames= guidance applies); threshold tuning
expected (phase 5 scalafmt precedent).


* Phase 19 — Cross-repository choreography impact             :STATUS:todo:

Problem: in a choreography of services the blast radius crosses
repository boundaries, but review — phases 14-18 included —
stops at the repo border, and only the reviewer carries the
system knowledge.  Practitioner interviews on microservice API
evolution found the leading cause of breaking changes is the
weakness of MANUAL error detection: reviewers only know the
codebase in front of them (Lercher et al. 2024, via Cerny et
al. 2025).  Phase 1 already solved the hard part:
=code-review-projects-root= locates and clones repos by remote
URL, so the sibling clones under it are the locally available
choreography scope — for free.

Design:

- Scope: all sibling git repos under
  =code-review-projects-root= (one and two levels deep, like
  the phase 1 clone matching); a defcustom can declare an
  explicit scope.  The graph covers only repos PRESENT LOCALLY,
  and the UI must say so ("6 of ~40 org repos") instead of
  implying completeness.
- Coupling hint graph, cheapest signal first (deterministic,
  async, TTL-cached: the phase 14 harvest-layer shape):
  1. Declared edges: sibling dependency manifests (package.json,
     go.mod, pyproject.toml, pom.xml, Cargo.toml, Gemfile) whose
     dep names match sibling repos; spec files (OpenAPI,
     AsyncAPI, .proto, GraphQL schema) are the published
     contracts.  Pure file reads.
  2. Contract diff at review time: a PR that touches a spec
     file is diffed base vs head against a deterministic rule
     set (path or operation removed, response status removed,
     field required-ified, event payload field removed, topic
     renamed).  The oasdiff/buf breaking insight, in-process,
     phase-5-style pure diff functions.
  3. Consumer search: bounded =git grep -l= across sibling
     worktrees for changed endpoints, route paths, event names
     and renamed public symbols; jump buttons on hits.  Phase 1
     clones are =--filter=blob:none= (the harvest gotcha):
     fetch refs first, cap the probe, run ON demand like the
     phase 5 bounded greps.
  4. Ticket-key co-mining (HINT-GRADE): =git log --grep= per
     sibling for this PR's issue-tracker keys.  Co-change
     evidence is project-dependent (Price et al. 2026), so it is
     displayed as "historically coupled via TICKET-123", never
     as a warning.
- Surface: a "Choreography impact" Analysis section, one entry
  per coupled sibling (edge type, hit count, jump buttons), plus
  optional =[XREPO]= hunk tags.  Off by default, one toggle,
  cached per PR+diff like phase 5.  Local diff reviews (phase 12
  constraint) work as far as siblings exist locally.

Use case — what this catches in review:

- A PR renames a topic or an event payload field in an AsyncAPI
  spec: the contract diff flags the BREAKING change and consumer
  search names the sibling subscribers, before anyone stands up
  an integration environment.
- A PR deletes an endpoint: the reviewer sees which sibling
  repos call that route (jump buttons), so the right team gets
  pinged BEFORE merge, not after an incident.
- A PR "adds a feature": ticket-key mining notes the same feature
  historically touched repos B and C, so the reviewer checks
  them for the same half-done change.
- An agent PR silently narrows a shared schema: caught
  deterministically, with no AI and no network in the loop.

Caveats: freshness is bounded by the sibling clones (fetch refs
on first use; full-text grep lazy-fetches blobs in partial
clones, so it stays capped and on-demand).  Evidence is thinner
than single-repo heat — score 7.5/10, value depends on org
topology and local repo availability — but the contract-diff
sub-piece is as evidence-backed as anything in this file.
Prior art: Cerny et al. (arXiv 2501.11778) prove static
cross-repo impact analysis works (endpoint-signature matching
plus data overlaps, validated on a 47-service benchmark) but
need framework-tailored AST parsing — research-grade; we borrow
the idea, not the machinery.  The shipped simple-and-effective
versions are contract breaking-change gates (oasdiff, buf
breaking, asyncapi/diff) and cross-repo code search; AI
multi-repo review (CodeRabbit, Qodo 2026) exists but is the
non-deterministic flavor we avoid.  Sources in the References
section.


* Future ideas

- =code-review-repo-cleanup=: prune old worktrees (older than N
  days).
- =code-compass= integrate information from code-compass analyses to prioritize review

  things like addding to an hotspot file, or the author not having experience with the module should contribute to inform the reviewer of how much attention to put in the review or what bits are most critical

  (Designed 2026-09-24 as phases 14-18: the heat reading order
  and the coupling cross-check consume code-compass's code-maat
  analyses when present, or a pure-elisp harvest otherwise; see
  the shared harvest layer under phase 14.)
- Commit buffer should reuse the same worktree context.

DONE since: per-PR review buffers
(=*Code Review: owner/repo#N*=, fallback =*Code Review*=) so
several reviews stay open at once.  Each buffer remembers the
database id of its PR; a buffer-local =post-command-hook= makes
the DB current pullreq follow the buffer you act in, and comment
buffers record the review buffer they were opened for.  The
review/worktree locals are =permanent-local= so the
=(code-review-mode)= activation that follows rendering does not
wipe them.

* References — phases 14-19 research (2026-09-24)

Sources consulted in the 2026-09-24 research sessions that
shaped phases 14-19; each entry notes the claim it grounds.

** Behavioral code analysis (phases 14, 16, 18)

- Nagappan & Ball, "Use of Relative Code Churn Measures to
  Predict System Defect Density", ICSE 2005.
  https://ieeexplore.ieee.org/document/1553571/
  Phase 14's core evidence: churn predicts defect density.

- CodeScene hotspots documentation ("What is a Hotspot?",
  "hotspots point to code review candidates", "Use Defects to
  put Costs on Hotspots" with the 1.2% of code / 12.5% of
  effort / 45% of bugs dashboard numbers).
  https://codescene.io/docs/guides/technical/hotspots.html

- CodeScene change-coupling documentation (coupling degree as a
  percentage; POSITIVE coupling between a source file and its
  tests — phase 18's star check).
  https://codescene.io/docs/guides/technical/change-coupling.html

- Adam Tornhill, "Your Code as a Crime Scene" and "Software
  Design X-Rays", Pragmatic Bookshelf — the method code-maat and
  CodeScene automate (churn x complexity hotspots, temporal
  coupling, knowledge maps; history as the missing context).
  https://pragprog.com/titles/atcrime/your-code-as-a-crime-scene/
  https://pragprog.com/titles/atevol/software-design-x-rays/

- code-maat — the analysis engine code-compass wraps (analyses:
  revisions, authors, main-dev, fragmentation, coupling; -i
  min-coupling default 30; --max-changeset-size; the git2 log
  format guidance).
  https://github.com/adamtornhill/code-maat

- code-compass — the installed Emacs frontend (async code-maat
  runs, CSV cache, coupling alist hook; the integration surface
  phases 14/18 plan to read).
  https://github.com/ag91/code-compass

** Just-in-time (change-level) defect prediction (phases 14, 15)

- "Just-in-time defect prediction for software hunks", Software:
  Practice and Experience, 2021.
  https://onlinelibrary.wiley.com/doi/10.1002/spe.3001
  Phase 15's premise: the JIT signal family works at HUNK
  granularity, not just file or commit.

- "Effort-aware and just-in-time defect prediction with neural
  network", PLOS ONE, 2019.
  https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0211359
  Ranks changes by defect likelihood DIVIDED BY inspection
  effort — the formal version of a review reading order.

** Change coupling and defects (phase 18)

- D'Ambros & Lanza, "On the Relationship Between Change Coupling
  and Software Defects", 2009.
  https://www.inf.usi.ch/lanza/PUBS/DAmb2009e.pdf

- Wiese et al., "An Empirical Study of the Relation Between
  Strong Change Couplings and the Number of Defects", 2015.
  https://link.springer.com/chapter/10.1007/978-3-319-17837-0_1

- "The relationship between evolutionary coupling and defects in
  large industrial systems", Software Maintenance and Evolution:
  Research and Practice, 2017.
  https://onlinelibrary.wiley.com/doi/full/10.1002/smr.1842

** Reviewing agent-generated PRs (phases 14, 16, 17)

- GitHub Blog, "Agent pull requests are everywhere. Here's how
  to review them." (2026-05-07) — CI gaming as red flag #1,
  code-reuse blindness as "the highest-ROI thing you can do as a
  reviewer", "trace one critical path", "require a test that
  fails on the pre-change behavior", agents carry "zero context
  about your incident history, your team's edge case lore".
  https://github.blog/ai-and-ml/generative-ai/agent-pull-requests-are-everywhere-heres-how-to-review-them/

- "More Code, Less Reuse" (arXiv, Jan 2026) — agent-generated
  code introduces more redundancy and quiet technical debt per
  change, and reviewers feel BETTER about approving it.
  https://arxiv.org/abs/2601.21276

- Duma, Wróblewski, Bobińska, Winiarska & Przymus, "These
  Aren't the Reviews You're Looking For: How Humans Review
  AI-Generated Pull Requests", EASE 2026 — 33,596 agent PRs:
  61% no recorded review, 84% no observable human involvement.
  https://arxiv.org/abs/2605.02273

- "Early-Stage Prediction of Review Effort in AI-Generated Pull
  Requests" (arXiv, Jan 2026) — even static creation-time
  features (patch size, file types) carry review-effort signal:
  deterministic pre-ranking is not guesswork.
  https://arxiv.org/abs/2601.00753

- PR Lens, "Reviewing AI-generated pull requests: the complete
  guide" (2026) — the practitioner review order (read the
  shape, check CI, hunt duplicated code, trace one path, test
  the boundaries, then read lines) that phases 14-17 mechanize.
  https://prlens.dev/guides/reviewing-ai-generated-pull-requests

** On AI assistance in review (phase 16's restraint)

- Alami & Ernst, "Human and machine: how software engineers
  perceive and engage with AI-assisted code reviews compared to
  their peers" (2025) — AI feedback reduces social friction but
  adds the cognitive load of VALIDATING it.
  https://arxiv.org/abs/2501.02092

- Tufano, Dabić, Mastropaolo, Ciniselli & Bavota, "Code review
  automation: strengths and weaknesses of the state of the art"
  (2024) — automated review handles simple changes, fails where
  broad repository context is needed — i.e. the delicate parts.
  https://arxiv.org/abs/2401.05136

** Test impact analysis practice (phase 17)

- Symflower, "Test impact analysis: Automatically run affected
  tests only" (2024) — ~29% average test-time savings from
  running only the affected tests.
  https://symflower.com/en/company/blog/2024/test-impact-analysis/

- Microsoft Learn, "Use Test Impact Analysis - Azure Pipelines"
  — change-based test selection as shipped industrial practice.
  https://learn.microsoft.com/en-us/azure/devops/pipelines/test/test-impact-analysis

** Cross-repository review scope (phase 19)

- Cerny, Goulis & Abdelfattah, "Towards Change Impact Analysis
  in Microservices-based System Evolution" (2025) — static
  per-repo component parsing, cross-connected by endpoint
  signatures and data overlaps; flags cross-service breakage at
  review time; the reviewer-blindness motivation ("all they
  know is the microservice codebase").
  https://arxiv.org/abs/2501.11778

- Lercher, Glock, Macho & Pinzger, "Microservice API evolution
  in practice: A study on strategies and challenges", Journal
  of Systems and Software, 2024 — practitioner interviews: the
  leading cause of breaking changes is weak MANUAL error
  detection (no open URL; cited via Cerny et al.).

- CodeRabbit, Multi-Repo Analysis documentation (2026) and
  Qodo, "The Anatomy of a Breaking Change" (2026) — commercial
  AI review tools that detect cross-repo downstream impact at
  review time: proof the blind spot is real, not the model for
  this phase (non-deterministic flavor).
  https://docs.coderabbit.ai/knowledge-base/multi-repo-analysis
  https://www.qodo.ai/blog/the-anatomy-of-a-breaking-change/

- oasdiff — OpenAPI breaking-change gates at PR time (755
  rules; GitHub Action posting a review on every PR).
  https://www.oasdiff.com/

- Buf, "Detecting breaking changes" — the same gate for
  Protobuf schemas.
  https://buf.build/docs/breaking/

- asyncapi/diff — the same gate for AsyncAPI event documents:
  the choreography-native case (topics, event payloads).
  https://github.com/asyncapi/diff

- "Ensuring Syntactic Interoperability Using Consumer-Driven
  Contract Testing", Software Testing, Verification and
  Reliability, 2025 — systematic literature review:
  consumer-driven contracts (Pact-style) ensure syntactic
  compatibility between services.
  https://onlinelibrary.wiley.com/doi/10.1002/stvr.70006

- Backstage, "System Model" — DECLARED service-to-service
  relationships (dependsOn) as shipped org-level coupling maps.
  https://backstage.io/docs/features/software-catalog/system-model/

- Sourcegraph, "Monorepo vs Polyrepo" — cross-repo code search
  as the polyrepo org's review-side tool.
  https://sourcegraph.com/blog/monorepo-vs-polyrepo

- Price, Cutting & Garousi, "Is Code Co-Committal an Indicator
  of Evolutionary Coupling in Software Repositories?", Software
  (MDPI), 2026 — co-change links vary widely with project and
  developer style; coupling evidence only "in certain
  scenarios".  Grounds phase 19's hint-grade treatment of
  history mining (and cautions phase 18's thresholds).
  https://doi.org/10.3390/software5010011
